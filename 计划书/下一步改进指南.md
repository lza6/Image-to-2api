# 听风AI · 下一步改进指南（v8.5.0 → v9.0.0 / v9.5.0 深化跃迁全景蓝图）

> **文档定位**：交给执行方（其他 AI 或开发者）的可落地迭代指南。每条改进锚定到具体 `file:line`、函数名、验收命令。遵循「不重构、只改进；不造轮子、只接线/扩面/深化；先测后改、小步快跑；真实闭环、凡不附真实输出的"完成"都是伪实现」四铁律。
>
> **本次为基于 v8.5.0 真实基线的全面重写**：旧版指南基线停留在 v8.2.3，已严重过时——v8.0.0（P0 架构治理）、v8.1.0（agent 子系统）、v8.2.x（CI 历史遗留根治）、v8.3.0（storage 热路径真接线）、v8.5.0（大文件拆分收尾 + agent E2E + 运营层 + 前端登顶 + 向量检索 + 成本预测）均已闭环。本版以 **v8.5.0 真实基线** 重画画像，区分「已落地勿重做 / 真实缺口收尾 / v9.0.0 新跃迁」三类，避免执行方把已完成的当待办重做。
>
> **生成日期**：2026-09-07
> **基线版本**：v8.5.0（`pyproject.toml:4` `version = "8.5.0"`；5 处一致：`pyproject.toml:4` + `api/main.py` + `frontend/package.json:4` + `landing/package.json:3` + `deploy/docker-compose.yml:3`；README.md:9 badge 滞留 v8.3.0 属文档漂移，见 §5.8 N1）
> **基线测试**：148 个 test 文件 / 1857 个 `def test_`（unit≈135 顶层文件 / 1806 用例 + integration 13 文件 / 45 用例 + chaos 3 + performance 3）；**实测最近一次全量全绿基线**为 `.benchmarks/v81full2.xml`（2026-09-06 00:39）= **1723 passed + 1 skip + 集成 37 + 混沌 3 + 基准 3**（memory 索引"1742 单测全绿"为计划书引用值，实测为 1723，见 §12.6 修正）。CI 口径 `pytest -m "not integration and not chaos and not slow" --cov-fail-under=80`
> **上游档案**：本指南消费 `计划书/2026-09-06-stage-mtpswcm8-权威基线档案.md`（5 缺口收敛）+ `计划书/2026-09-07-stage-mtpst934-全栈现状分析.md`（5 新发现）+ `计划书/全阶段落地闭环完整的步骤+多agent子代理并行计划.md`（6 工作流）+ `计划书/全阶段落地闭环-步骤拆解细则.md`（原子步骤），不重复其内容，只在其上做 v9.0.0 跃迁编排。

---

## 0. 文档信息

| 项 | 值 |
|---|---|
| 项目名称 | 听风AI / imagefree-2ai（多提供商 AI 图像/对话生成网关） |
| 仓库 | `C:\Users\Administrator.DESKTOP-EGNE9ND\Desktop\imagefree-2ai`（main 分支） |
| 分析日期 | 2026-09-07 |
| 当前分支 | main |
| 当前版本 | v8.5.0（commit `accda34`） |
| 目标版本 | v9.0.0（深化跃迁）/ v9.5.0（长期演进） |
| 本次分析范围 | `api/` 后端 + `frontend/` React 管理面板 + `landing/` Vue 落地页 + `deploy/` 部署 + `tests/` + `.github/workflows/` + `pyproject.toml` + 项目文档（`docs/`、`README.md`、`.wolf/`、`计划书/`） |
| 未分析范围 | `可参考的项目/`（参考仓库群）、`landing/node_modules/`、`.venv/`、`graft/.cache/`（图谱缓存）、`data/*.db`（运行时数据）、`.benchmarks/` 中间产物 —— 均非源码 |
| 证据来源 | 实际读取的源码 + `git log --oneline -50` + `rg`/`Glob` + `.wolf/cerebrum.md` + `.wolf/anatomy.md` + `计划书/` 四份上游档案 + `pyproject.toml` + `README.md` |
| 验证环境 | Windows 10 Pro / Git Bash / Python 3.11；**只读静态审查**，未运行测试套件（本次授权仅产文档，不跑不装） |
| 文档状态 | 初稿，基于 v8.5.0 真实代码库全面重写；旧版 v8.2.3 基线段已替换 |

**验证状态声明（重要）**：
- 凡称「已落地」者，均经源码静态确认（`rg`/`Read`/`Glob`）存在且被调用，并在 §4「可复用资产」列出 file:line。
- 凡称「真实缺口」者，均给出 `Glob`/`rg` 确认符号或目录不存在的证据（G1-G5 见 §5.1-§5.5，N1-N5 见 §5.6-§5.10）。
- 凡称「v9.0.0 新跃迁」者，均标注「新建议」，并给出复用的现有轮子位置（不造轮子）。
- **本次未运行 pytest/vitest/build/mypy/ruff/node**——非因无法运行，而是本次任务授权边界为「只产文档」。下文所有「验收命令」是给执行方的待跑清单，**本指南作者未替你跑过**，执行方落地时须自行运行并附真实输出（反伪实现铁则）。

---

## 1. 执行摘要

### 1.1 项目所处阶段

听风AI 已过「0→1」（初创）和「1→2」（架构治理）阶段，处于 **「2→3」的能力深化与运营登顶阶段**，正向 **v9.0.0 深化跃迁** 推进：

- **架构治理已收尾**（v8.0.0+v8.5.0 P0）：大文件已全部拆分到 <800 行（`config/__init__.py` 1178→769、`account_pool/pool.py` 1111→75、`db/core.py` 985→371、`worker/engine.py` 833→738 + scaler 261 + dlq 56）。`api/` 150 个 py 文件，无 >1000 行文件。
- **agent 子系统已落地并闭环**（v8.1.0+v8.5.0 P1-A）：`api/agent/`（intent/critic/guard/memory/routes/metrics）+ `/v1/agent/*` 端点 + E2E 测试 + 4 个 Prometheus 指标；intent/critic/memory 均有真实 LLM 调用路径（走 tryingopen 免费上游，`IF_MOCK_UPSTREAM=0` 默认真实，非 Mock 悬空）。
- **路由算法已升级**（v8.0.0 P1-3）：`adaptive_router.py` MAB-EWMA + Thompson Sampling + UCB1 已实现并接 `select_provider()`，路由决策 SQLite 持久化 + warm 冷启动。
- **实时通道已增强**（v7.7.17 P1-6）：`api/ws_events.py` + `/v1/tasks/{id}/ws` WebSocket 双向通道已落地；SSE 每任务事件流 + Last-Event-ID 断线补偿。
- **storage 热路径已真接线**（v8.3.0 P0-S1）：`request_guard.py` 真调 `adapter.rate_limiter.is_allowed`，降级内存桶（不 fail-open 保真限流）。
- **免费运营三步走已落地**（v8.5.0 P1-O）：litestream（RPO 秒级）+ UptimeRobot（拨测告警）+ Grafana（仪表盘 + SLO 错误预算）+ compose 三 profiles（redis/obs/backup）。
- **前端登顶组件已落地**（v8.5.0 P2-C）：Skeleton/EmptyState/CommandPalette + useOptimisticMutation/useVirtualList + a11y-audit.cjs + perf-audit.cjs + landing LCP<1.5s + a11y 0 critical。
- **向量检索 + 成本预测已落地**（v8.5.0 P3-D）：`api/vector/{embed,store}.py`（SimHash 256 维 + sqlite-vec 降级）+ `/v1/gallery/similar` + `cost_forecast.py` + Costs.tsx 预算燃烧预测。
- **CI 历史遗留已根治**（v8.1.1→v8.2.3 P0-11）：`.gitignore` 误伤 presets.py、lock 漂移、ruff 排序遗漏等 13 轮诊断链闭环；flaky `test_account_growth` 已加 8s 最终一致性轮询根治。

### 1.2 最值得保留的优势（护城河）

1. **四铁律执行到位**：不重构公共接口（拆分留兼容垫片 `from .new_module import *`）、不造轮子（前瞻能力造好待接线）、先测后改（TDD + AST 契约测试 `test_async_sync_contamination.py`）、真实闭环（凡"完成"附真实输出）。
2. **前瞻能力储备丰厚**：`api/storage/`（Redis 分布式锁 + 滑窗限流 + token 校验，热路径已接）、`api/agent/`（意图识别 + 记忆巩固 L0-L3 + critic 审查 + guard 风控）、`api/solver_guard.py`（多节点联邦 + 熔断 + IdleTimeout）、`api/vector/`（向量检索）、`api/adaptive_router.py`（Thompson/UCB1 + 持久化）—— 均已造好，v9.0.0 只需深化/扩面。
3. **公益开放 + 单机可部署双不变量**：生图/聊天不限 Key（`guard_generate_request`/`guard_chat_request` 已移除 `check_api_key`），外部依赖全可选（`IF_STORAGE_BACKEND=sqlite` 缺省零依赖，Docker Compose 不带 `--profile` 仅 api+cfsolver）。
4. **版本一致性契约**：5 处版本号强一致（pyproject×1 + api/main.py + frontend/package.json + landing/package.json + deploy/docker-compose.yml），CI `frontend-version-gate` 门禁。
5. **真实闭环文化**：`.wolf/cerebrum.md`（Do-Not-Repeat / Key Learnings / Decision Log）+ `docs/verification-log.md`（验证过勿重跑）+ `计划书/` 多份基线档案，跨会话不重复踩坑。
6. **可观测性地基**：Prometheus `/v1/metrics` + OTel tail-based 采样（错误 100%+正常 10%）+ SSE/WS 事件流 + `slo_budget.py` 错误预算 + 慢日志 + 14 天落盘日志 + 审计 trace_id 透传。

### 1.3 最重要的核心目标（下一阶段，可测量）

| 序 | 问题 | 一句话 | 优先级 |
|---|------|--------|--------|
| ① | **真实缺口收尾** | G1 mypy strict 扩面 + G2 .benchmarks 清理 + G3 landing SW + N1 文档对齐 —— 5 项低工时高价值收尾 | P0-P2 |
| ② | **智能体 DAG 编排深化** | G4 基础 DAG（拓扑+并行+失败传播）→ v9.0.0 深化（多模态融合 + RAG + 工具链 + 自反思） | P3→P1* |
| ③ | **MCP 协议化** | agent 工具调用从私有协议升级到 MCP（Model Context Protocol），对外可被发现/可组合 | P2 |
| ④ | **设计系统统一** | 前端 14 页从"登顶组件"升级到"设计系统"（design tokens + compound components + 主题） | P2 |
| ⑤ | **运营登顶** | cf_solver 多节点扩容（G-ops）+ Cloudflare 接入（L3 待授权）+ 混沌工程常态化 + SRE Runbook | P1-P3 |

> *P3→P1：智能体 DAG 虽原标 P3，但 v8.5.0 已落地 agent E2E + 向量检索 + 成本预测三大前置，DAG 是把它们串联成"多步任务编排"的关键跃迁，建议提级为 v9.0.0 主打。

### 1.4 推荐总体实施顺序

```
第 1 波：真实缺口收尾（P0-P2，1-2 天，4 agent 并行）
  ├─ G1 mypy strict 扩面 db.core + db.queries（P0，8h）
  ├─ G2 .benchmarks 旧产物清理（P1，1h，脚本已就绪）
  ├─ N1 文档版本对齐（README badge + SOP + 计划书索引）（P1，1-2h）
  └─ N3 db/queries.py 804 行微拆（P2，1h）
        ↓
第 2 波：v9.0.0 核心跃迁（P1-P2，1-2 周，5 agent 并行）
  ├─ G4-Plus 智能体 DAG 编排深化（多模态 + RAG + 自反思）（P1，28h）
  ├─ MCP 协议化（agent 工具 → MCP server）（P2，12h）
  ├─ G3 landing Service Worker（P2，3h）
  ├─ 设计系统统一（design tokens + compound + 主题）（P2，16h）
  └─ cf_solver 多节点联邦扩容（G-ops，P1，1 天，本地 --scale 验证）
        ↓
第 3 波：运营登顶（P2-P3，1 周，4 agent 并行）
  ├─ Cloudflare 免费层接入（G5，L3 待授权，4h 方案）
  ├─ 混沌工程常态化（chaos/ 测试 + Gremlin 式故障注入）（P2，8h）
  ├─ SRE Runbook 自动化（deploy/docs/runbooks/）（P3，6h）
  ├─ 安全加固（CSP nonce + 密钥轮换 + SBOM/SLSA）（P2，10h）
  └─ 性能登顶（bundle 瘦身 + 代码分割 + SQLite 读写分离）（P2，10h）
        ↓
第 4 波：长期演进（P3，4+ 周滚动）
  ├─ 多租户隔离预留（单租户 → 多租户平滑路径）（P3）
  ├─ A2A 协议（agent-to-agent 互通）（P3）
  ├─ 边缘计算（CF Workers 前置轻逻辑）（P3，L3）
  └─ 成本智能调度（cost_forecast + adaptive_router 联动）（P3）
```

### 1.5 当前最大的交付风险

- **旧指南误导风险**：旧版《下一步改进指南.md》基线停留在 v8.2.3，列了 136h 待办，但权威基线档案证明 ~85% 已在 v8.5.0 落地。若执行方误用旧版，会把已落地项当待办重做（如 storage 热路径、大文件拆分、agent E2E、litestream、Grafana、向量检索、成本预测、flaky 根治）—— **本版已全面重写并覆盖旧版，执行方以本版为准**。
- **DAG 跃迁风险**：G4 智能体 DAG 是 v9.0.0 主打，但需复用 agent/vector/SSE/queue 四套子系统，若执行方不遵循"只追加不重构"铁律，可能破坏现有 agent 模块（intent/critic/memory）。**必须在新建 `api/agent/dag.py`/`planner.py` 时，不改现有 agent 模块**（见 §6.1 禁区）。
- **MCP 协议风险**：MCP 化涉及 agent 工具调用协议变更，若破坏现有 `/v1/agent/*` 端点契约，会回归。**必须保持 `IF_AGENT_*_ENABLED` 缺省关闭 + 双轨并存**（见 §6.2）。
- **生产授权风险**：Cloudflare 域名 NS 迁移 + cf_solver 生产扩容均属 L3，未经用户授权不实施（见 §5.4 G5 + §8.1 G-ops 决策边界）。
- **运行时验证缺口**：本指南所有"已落地"结论来自源码静态确认，**非来自运行测试**。执行方落地前必须先跑 `pytest -m "not integration and not chaos and not slow"` 建立回归基线，再动手。

---

## 2. 当前项目基线

### 2.1 项目目标与用户

听风AI 是「上游免费 AI 能力的聚合与再分发网关」：把 N 家上游的「人机验证（Turnstile）+ 账号额度 + IP 风控 + 异步生成」四重摩擦，压缩成开发者侧的一个 `POST /v1/generate` / `POST /v1/chat/completions` 同步调用。

**一句话本质**：核心价值 = 四重摩擦的消除效率。护城河与瓶颈完全一致，都在「四重摩擦消除」上。v9.0.0 的跃迁方向 = 把"单步调用"升级为"多步智能体编排"，把摩擦消除从"单点"升级到"链路"。

**目标用户**：开发者（调 OpenAI 风格 API）+ 站长（管理面板运营号池/代理池/Provider）。公益开放，生图/聊天不限 Key。

### 2.2 技术栈

| 层 | 栈 | 证据 |
|---|---|---|
| 后端 | Python 3.11 / FastAPI / uvicorn / httpx / aiosqlite / pydantic-settings / prometheus-client / OpenTelemetry（可选）/ sqlite-vec | `pyproject.toml:7-18`、`requires-python = ">=3.11"` |
| 前端管理面板 | React 19 + TypeScript 5.7 + Vite 6 + react-router-dom 7 + recharts + Vitest 4 | `frontend/src/pages/`（13 页）、`frontend/package.json:4 version=8.5.0` |
| 公开落地页 | Vue3 + Vite + Three.js + @tresjs + motion-v | `landing/package.json:3 version=8.5.0`、`landing/src/components/Hero3D.vue` |
| 外部求解器 | `cf_solver`（Turnstile 求解器，端口 8001），`deploy/cf_solver/boterdrop_wrapper.py` | `README.md:65` |
| 数据层 | SQLite（aiosqlite 异步）—— `data/imagefree.db` / `data/account_pool.db` / `data/email_registry.db` / `data/edit_leases.db` / `data/queue.db`（持久化队列）+ sqlite-vec 向量索引 | `api/db/core.py`(371)、`api/db/queue_store.py`、`api/vector/store.py` |
| 存储适配 | `api/storage/`（base/factory/local/redis_adapter）—— Redis 分布式锁 + 滑窗限流 + token 校验，热路径已接 `request_guard` | `api/request_guard.py:260,530` |
| Lint/类型 | ruff（select E/F/W/I/UP/B/SIM/ASYNC，0 error）/ mypy（渐进 strict，仅 `errors`+`retry_policy` 2 模块）/ pytest-asyncio | `pyproject.toml:27-53` |
| 部署 | Docker Compose（`deploy/docker-compose.yml`，api + cfsolver + redis/obs/backup 三可选 profile）+ litestream + Grafana + Prometheus | `deploy/docker-compose.yml:3 v8.5.0` |

### 2.3 目录与模块概览（v8.5.0 拆分收尾后）

```
api/
  main.py(<300)            应用组装入口（挂载路由/中间件/前端/生命周期/安全头），版本号在此
  routes/                  health/tasks/generate/admin/(query+write)/chat/ecosystem/security + agent 挂载
  dispatch.py              路由调度 + 路由记录全覆盖
  dispatch_edit.py         图生图双层互斥锁
  sse_events.py            每任务 SSE 事件流（subscribe/publish/replay + Last-Event-ID）
  ws_events.py             WebSocket 双向通道（/v1/tasks/{id}/ws）
  adaptive_router.py(770)  MAB-EWMA + Thompson + UCB1 路由引擎（接近 800 上限，v9 可考虑拆）
  lifespan.py              9+阶段优雅关闭 + storage 装配 + agent 记忆巩固 worker 启动
  worker/                  engine(738) / queue / dlq / generator / scaler(261) / token_pool(438)
  account_pool/            mixin 拆分：_base/_constants/borrow/store/signin/stats/engine/fsm/scoring + pool.py(75 兼容垫片)
  registerer/              注册器包：types/utils/flow/cf_solve/email_verify
  email_pool.py(384)+email_sources/  邮箱池 + 7 临时邮箱源
  providers/               base + imagefree/aifreeforever/nanobanana/tryingopen/(包化)+falai + registry + action_sniffer
  config/                  __init__.py(769) + base/cache/db/edit/http/observability/pool/provider/queue/security/solver/settings/presets
  db/                      core(371) / queries(804，微超 4 行) / migrations / queue_store / lease_store / ip_blocklist_store
  agent/                   intent(182)/critic(176)/guard(137)/memory(396)/routes(141)/metrics(113)（v8.1+v8.5 已落地）
  storage/                 base/factory/local/redis_adapter（v8.3 热路径已接）
  vector/                  embed/store（v8.5 已落地，SimHash + sqlite-vec 降级）
  solver_guard.py          cf_solver 多节点联邦 + 熔断 + IdleTimeout
  cf_clearance_solver.py   CF clearance 求解
  turnstile_client.py      Turnstile token 求解客户端
  captcha/                 统一 solver 抽象层（M12，可选）
  skills/                  skills 四件套体系（SKILL.md + scripts/ + references/ + assets/）
  cost_forecast.py         预算燃烧预测（v8.5 已落地）
  slo_budget.py            错误预算
  auth.py                  聊天端点固定 Key 鉴权（IF_API_KEYS）+ 管理面独立 Key（IF_ADMIN_KEYS）+ mask_key()
  request_guard.py         per-IP 分片锁限流 + XFF 防伪造 + 黑名单 + storage 热路径真接线
  telemetry.py             OTel tail 采样（错误 100% + 正常 10%）
  proxy_pool.py            住宅代理 + 免费代理双源轮换（健康评分 + 粘滞）
  background.py            spawn() 强引用集（fire-and-forget 统一入口）
  audit.py / alerting.py / metrics_ext.py / log_ws.py / log_buffer.py / disk_logger.py / slow_log.py / sse_stats.py / error_tracker.py / context.py
  prompts/                 提示词系统（base.md 宪法 + templates/ + loader + __init__ compose）
frontend/src/              pages/(13) + components/(Skeleton/EmptyState/CommandPalette + chat/3 + accounts/6) + hooks/(useOptimisticMutation/useVirtualList 等 4) + api/ barrel + 17 测试文件
landing/                   Vue3 + Three.js + @tresjs；9 个 Section 组件（Hero3D 等）
deploy/                    docker-compose.yml(3 profiles) / Dockerfile.api / Dockerfile.cfsolver / .env.example / .env.production.example / cf_solver/ / grafana/ / litestream.yml / prometheus.yml / docs/(monitoring + litestream-restore + cloudflare-cdn)
tests/                     unit(≈135) + integration(13) + chaos + performance + conftest.py
docs/                      SOP.md / PROVIDER_INTEGRATION_GUIDE.md / verification-log.md / architecture-evolution.md / prd/ / releases/
scripts/                   backup_db.py / restore_db.py / mock_cfsolver.py / e2e_*.py / loadtest.py / final_suite.py / _bench_cleanup.cjs（已就绪未执行）
.wolf/                     OPENWOLF.md / cerebrum.md / anatomy.md / memory.md / buglog.json（跨会话记忆）
计划书/                    迭代升级计划专目录（本文件 + 4 份上游档案 + 步骤拆解细则 + 多 agent 并行计划）
```

### 2.4 主要入口与运行路径

- **应用入口**：`api/main.py` `app = FastAPI(version="8.5.0", lifespan=lifespan)`
- **生图核心数据流**：调用方 → `POST /v1/generate` → `routes/generate.py` guard+prepare → SQLite 入库 → `dispatch.py` → `PriorityQueue` → `worker/engine.py`（worker 池 4-16 自适应 + scaler 多维评分）→ `providers/<provider>.py` → 上游；SSE/WS 反向推送状态。
- **聊天链路**：`POST /v1/chat/completions` → `routes/chat.py` → `providers/tryingopen/` → 上游 tryingopen.com（13+ 开源大模型，匿名 + 代理池轮换）。
- **agent 链路**：`POST /v1/agent/intent` → `agent/intent.py:_llm_classify` → `registry.all_chat_models()` → `provider.chat_collect` 走 tryingopen 免费上游（`IF_MOCK_UPSTREAM=0` 默认真实）→ 返回 IntentResult；`agent/memory.py` 记忆巩固 worker（lifespan 启动）做 L0-L3 巩固。
- **启动**：`python deploy/cf_solver/boterdrop_wrapper.py`（8001）+ `uvicorn api.main:app --port 8100`，或 `start.ps1` / `start.bat` 一键拉起。

### 2.5 测试与 CI 状态

| 维度 | 现状 | 证据 |
|---|---|---|
| 后端单测 | 148 个 test 文件 / 1857 个 `def test_`，CI 口径 `pytest -m "not integration and not chaos and not slow"`，覆盖率门禁 80% | `.github/workflows/ci.yml`、`pyproject.toml:60` addopts |
| 集成/混沌 | 分轮跑（v7.7 拆分防组合串扰），需先起 `mock_cfsolver` | `ci.yml`、`scripts/mock_cfsolver.py` |
| 前端 | Vitest（197+ 用例）+ tsc + build，CI `frontend-gate` 门禁 | `frontend/package.json`、`ci.yml` |
| E2E/响应式 | `e2e-smoke.cjs`（22 断言）+ `resp-audit.cjs`（20 断言，4 断点）+ `a11y-audit.cjs` + `perf-audit.cjs` | `frontend/` |
| ruff | 0 error（select E/F/W/I/UP/B/SIM/ASYNC 已引入） | `pyproject.toml:27-30` |
| mypy | 渐进 strict，仅 `api.errors` + `api.retry_policy` 两模块 0 error；`db/core.py`+`db/queries.py` 待扩面（G1） | `pyproject.toml:48-53` |
| 已知 flaky | `test_account_growth` 已加 8s 最终一致性轮询根治（v8.5.0） | `.wolf/cerebrum.md` 2026-09-06 条目 |

### 2.6 部署方式

- Docker Compose：`deploy/docker-compose.yml`（api + cfsolver + 三可选 profile：redis/obs/backup）。
- 线上：腾讯云东京，公益开放 `https://imagefree.tingfengai.art`（Caddy 反代 + 固定子网 `172.28.0.0/16` + `--proxy-headers` 恢复真实 IP）。**注**：记忆索引表明"服务器已过期/无远程部署"，链接有效性待确认（见 §5.9 N4）。
- **运营可观测性地基已就绪**：litestream（RPO 秒级）+ UptimeRobot（5min 拨测）+ Grafana（仪表盘 + SLO 错误预算）+ Prometheus —— v8.5.0 已全部落地。

### 2.7 已知限制（触发器未出现前的负优化）

`docs/architecture-evolution.md` 结论性指出，以下在触发器出现前**都是负优化，本指南不纳入**：

- 换 Postgres（数据 <百万行，SQLite 单文件轻松扛 10 年；sqlite-vec 向量索引也已就绪）
- 上 Kafka（最小集群 >2GB 内存，比 api 本身还重；已有 asyncio.PriorityQueue + 持久化队列开关）
- 分库分表（伪需求）
- Load Balancer（单实例部署，Nginx/HAProxy 只在 ≥2 实例时有意义；但 cf_solver 多节点联邦属真实瓶颈，见 §8.1）
- 引入 Redis 为强依赖（保持 `IF_STORAGE_BACKEND=sqlite` 缺省零依赖，Redis 仅可选集中式限流）

---

## 3. 已执行的检查与验证（静态侦察）

| 检查项 | 命令或操作 | 结果 | 状态 | 证据/说明 |
|---|---|---|---|---|
| git 状态 | `git status --short` | `.benchmarks/` + `graft/` 缓存产物 M/??（非源码） | 已验证 | 任务前快照 + 本指南不改源码 |
| 当前分支 | `git branch --show-current` | main | 已验证 | — |
| 近 50 提交 | `git log --oneline -50` | v8.0.0→v8.3.0→v8.5.0 演进链清晰可见 | 已验证 | commit `accda34 feat(v8.5.0)` 为主基线 |
| 基线版本 | `rg "8\.5\.0" pyproject.toml api/main.py frontend/package.json landing/package.json deploy/docker-compose.yml` | 5 处命中 `8.5.0` | 已验证 | 版本一致性契约达标 |
| README badge | `rg "version-" README.md` | `version-8.3.0`（滞后） | 静态确认 | 文档漂移，见 §5.8 N1 |
| ruff 现状 | `rg "select\b" pyproject.toml` | select 规则族 E/F/W/I/UP/B/SIM/ASYNC 已引入 | 静态确认 | v7.7.15 P0-9 已闭环 0 error |
| 大文件清单 | （上游档案 `wc -l` 实测） | `config/__init__.py`(769)、`account_pool/pool.py`(75)、`db/core.py`(371)、`worker/engine.py`(738) 全 <800；`adaptive_router.py`(770) 接近上限；`db/queries.py`(804) 微超 4 行 | 静态确认 | 见 §5.7 N3 |
| agent 子系统 | `ls api/agent/` + `rg agent api/routes/__init__.py` | intent/critic/guard/memory/routes/metrics 存在且挂载 `/v1/agent/*` | 已验证 | v8.1.0+v8.5.0 已落地 |
| agent LLM 真调 | `rg "provider.chat_collect\|chat_provider.chat_collect" api/agent/*.py` | 命中 intent.py / critic.py / memory.py（真实调用点） | 静态确认 | 非Mock悬空，IF_MOCK_UPSTREAM=0 默认真实 |
| storage 热路径 | `rg "adapter.rate_limiter.is_allowed\|_storage_adapter" api/request_guard.py` | L260/L530 真调适配器，非仅装配 | 已验证 | v8.3.0 P0-S1 已落地 |
| adaptive_router 算法 | `rg "thompson\|ucb" api/adaptive_router.py` | Thompson/UCB1 已实现并接 `select_provider()` | 已验证 | v8.0 P1-3 已落地 |
| ws_events 存在 | `ls api/ws_events.py` + `rg "/v1/tasks/.*/ws" api/routes/tasks.py` | WebSocket 端点已挂载 | 已验证 | v7.7.17 P1-6 已落地 |
| vector/ 目录 | `ls api/vector/` | embed.py + store.py 存在 | 已验证 | v8.5.0 P3-D1 已落地 |
| grafana/ 目录 | `ls -d deploy/grafana/` | 存在（dashboard + provisioning） | 已验证 | v8.5.0 P1-O4 已落地 |
| litestream | `ls deploy/litestream.yml` | 存在 | 已验证 | v8.5.0 P1-O1 已落地 |
| compose profiles | `rg "profiles:" deploy/docker-compose.yml` | redis/obs/backup 三 profile | 已验证 | v8.5.0 P1-O5 已落地 |
| mypy strict 名单 | `rg "module = \[" pyproject.toml` | 仍 2 模块（`api.errors`+`api.retry_policy`） | 静态确认 | G1 真实缺口 |
| 智能体 DAG | `ls api/agent/dag.py api/agent/planner.py` | 均不存在 | 静态确认 | G4 真实缺口 |
| landing SW | `ls landing/sw.js` | 不存在 | 静态确认 | G3 真实缺口 |
| .benchmarks 项数 | `ls -A .benchmarks \| wc -l` | 42（含 4 张已跟踪 resp-shots png + 2 e2e png + 39 中间产物） | 静态确认 | G2 真实缺口，`scripts/_bench_cleanup.cjs` 已就绪 |
| Cloudflare 方案 | `ls deploy/docs/cloudflare-cdn.md` | 存在（172 行，L3 待授权） | 静态确认 | G5 方案就绪，实施待授权 |
| db/queries.py 行数 | `wc -l api/db/queries.py` | 804（微超 800 上限 4 行） | 静态确认 | N3，微拆或豁免均可 |
| 后端单测全量运行 | `pytest -m "not integration and not chaos and not slow"` | **未运行** | 待验证 | 本次授权仅产文档；执行方落地时须自行运行并附输出 |
| 前端 vitest/build | `cd frontend && npm run test && npm run build` | **未运行** | 待验证 | 同上 |
| 真实用户路径（浏览器） | Playwright 打开 /admin 各页点击 | **未运行** | 待验证 | playwright MCP 本次连接失败，无法做浏览器验证；执行方须自行验证 |

**重要声明**：本指南所有「已落地」结论来自源码静态确认（`rg`/`Read`/`Glob`）+ 上游 4 份档案交叉印证，**非来自运行测试**。下文验收命令清单是给执行方的待跑项，本指南作者未替你跑过 —— 这符合本次「只产文档，不跑不装」的授权边界，也符合反伪实现铁则。

---

## 4. 现有优势与可复用资产（不重复造轮子）

> 提出任何新增能力前，先确认以下已存在，优先**接线/复用/扩面/深化**。v9.0.0 跃迁必须建立在这些轮子之上，而非另起炉灶。

| 能力 | 位置 | 状态 | v9.0.0 复用方式 |
|---|---|---|---|
| Redis 分布式锁 + 滑窗限流 + token 校验 | `api/storage/redis_adapter.py` + `factory.py` | **热路径已接**（v8.3.0） | DAG 节点互斥、多 agent 协调锁直接调 `adapter.acquire_token`，不重写 |
| agent 意图识别 + 记忆巩固 L0-L3 + critic + guard | `api/agent/intent.py`/`memory.py`/`critic.py`/`guard.py` | **已落地且有真实 LLM 路径** | DAG planner 复用 intent 做任务分解 + critic 做节点终检 + memory 做 run 记忆，不重写 |
| cf_solver 多节点联邦 + 熔断 + IdleTimeout | `api/solver_guard.py` | 代码层就绪 | 扩容只加 `IF_CF_SOLVER_URLS` + compose replicas，不改逻辑（见 §8.1） |
| 路由决策 SQLite 持久化 + warm 冷启动 + Thompson/UCB1 | `api/adaptive_router.py`(770) | 已实现 | DAG 节点选 provider 复用 `select_provider()`，不重写引擎；adaptive_router 本身接近 800 上限，v9 可考虑拆分（见 §7.6） |
| 持久化队列开关 | `api/worker/engine.py` + `db/queue_store.py` | 已实现（`IF_PERSISTENT_QUEUE_ENABLED`，默认开） | DAG run 入队复用 queue_store 持久化模式，不另起 |
| WebSocket 双向通道 + SSE 事件流 | `api/ws_events.py` + `sse_events.py` | 已挂载 `/v1/tasks/{id}/ws` + `/v1/tasks/{id}/events` | DAG 节点状态推送直接复用 publish/broadcast，不另起实时通道 |
| background.spawn() 强引用集 | `api/background.py` | v7.7 已统一 fire-and-forget | 所有新后台任务（DAG 执行器、记忆巩固）直接 spawn，禁裸 `asyncio.create_task` |
| Prometheus /v1/metrics + OTel tail 采样 | `api/telemetry.py` + `metrics_ext.py` | 已就绪 | DAG/agent 新指标直接 Counter/Histogram，Grafana 自动消费 |
| SLO 错误预算 | `api/slo_budget.py` | 已实现 | Grafana SLO 仪表盘已消费，不另起 |
| 反滥用自动封禁 | `engine._finish` task-failure-burst 违规记录 | v7.7.4 已落地 | 封禁 403 附申诉渠道（`IF_ADMIN_CONTACT`），DAG 节点失败也走此路径 |
| 安全响应头中间件 | `api/main.py SecurityHeadersMiddleware` | 已就绪（HSTS/XCTO/XFO/RRP + 可选 CSP） | CSP nonce 化只需扩常量（见 §9.2） |
| SPA 深链回退 + index.html no-cache | `api/main.py SPAStaticFiles` | v7.7.11 已根治 vite 新 chunk 卡死 | 勿再加重复 no-cache 逻辑 |
| 前端登顶组件 | `frontend/src/components/{Skeleton,EmptyState,CommandPalette}.tsx` + `hooks/{useOptimisticMutation,useVirtualList}.ts` | v8.5.0 已落地 | 新页面直接复用，不重写骨架/空态/命令面板 |
| 前端 a11y 地基 | `frontend/src/` 全局样式 + `a11y-audit.cjs` | v7.6+v8.5 已落地 | 页面级只补 input aria-label，勿重复加全局样式 |
| 前端自动化审计 | `a11y-audit.cjs` + `perf-audit.cjs` + `resp-audit.cjs` | v8.5.0 已落地 | CI 直接挂，新页面自动覆盖 |
| 邮箱池拆分先例 | `api/email_pool.py`(384) + `email_sources/`(7 文件) | v7.3 立的拆分模式 | account_pool/config/db.core 已照此拆分；adaptive_router 拆分也照此 |
| AST 契约测试 | `tests/test_async_sync_contamination.py` | 已存在，扫描 account_pool/email_pool/nanobanana/aifreeforever/imagefree | 改这些文件后必跑 |
| 向量检索 | `api/vector/{embed,store}.py` + `/v1/gallery/similar` | v8.5.0 已落地（SimHash + sqlite-vec 降级） | DAG 节点可调向量检索做语义去重/相似聚类；RAG 直接复用 embed，不另起向量栈 |
| 成本预测 | `api/cost_forecast.py` + `Costs.tsx` | v8.5.0 已落地（predict_budget_burn） | 智能调度复用：路由引擎选 provider 时把成本预测纳入打分（见 §11.5） |
| 提示词系统 | `api/prompts/`（base.md 宪法 + templates/ + loader + compose） | 已落地 | DAG planner 的 system prompt 走 compose_system_text，不硬编码 |
| skills 四件套 | `api/skills/`（SKILL.md + scripts/ + references/ + assets/） | 已落地 | MCP 化时把 skills 暴露为 MCP tools，不另起技能框架 |
| 统一 captcha 抽象 | `api/captcha/`（M12） | 可选就绪 | 新 provider 复用，不另起 solver 抽象 |

---

## 5. 真实缺口收尾（G1-G5 + N1-N5，引用上游计划，不重做）

> 本节是 v8.5.0 真实缺口的**收尾清单**。每项均有上游档案的原子步骤展开，本指南**不重复展开**，只做 v9.0.0 编排。执行方须先消费 `计划书/全阶段落地闭环-步骤拆解细则.md` §1-§6 的原子步骤，再进入本指南 §6+ 的 v9.0.0 跃迁。

### 5.1 G1：mypy strict 扩面 db.core + db.queries（P0，~8h）

- **缺口**：`pyproject.toml:49` strict 名单仍仅 2 模块（`api.errors`+`api.retry_policy`）；`db/core.py` 预存注释"215 处 strict 错误"为旧值，v8.5.0 拆分后应大幅下降，**数值待验证**。
- **原子步骤**：见 `计划书/全阶段落地闭环-步骤拆解细则.md` §1（A1-01 ~ A1-16，含错误数实测 → 逐类修/分批收敛 → 扩面 → ruff/CI 回归）。
- **验收**：`mypy --strict api/errors.py api/retry_policy.py api/db/core.py api/db/queries.py` 0 error + `pytest tests/test_db_*.py` 全绿。
- **回滚**：`git checkout pyproject.toml api/db/core.py api/db/queries.py` → strict 名单回 2 模块。
- **v9.0.0 衔接**：扩面后建议继续把 `api/agent/`、`api/vector/`、`api/storage/` 纳入 strict（新模块直接 strict，见 §6.1 D1-18）。

### 5.2 G2：.benchmarks 旧产物清理（P1，~1h，脚本已就绪）

- **缺口**：`.benchmarks/` 42 项（19 xml + 6 txt + 11 log + 3 zip/json + `resp-shots/` 4 png + 2 e2e png）；`scripts/_bench_cleanup.cjs` 已就绪未执行。
- **原子步骤**：见 `计划书/全阶段落地闭环-步骤拆解细则.md` §2（A2-01 ~ A2-11，含归属二次确认 → 删除 39 中间产物 → .gitignore 防再积 → resp-audit 回归）。
- **验收**：`ls .benchmarks/` 仅剩 `resp-shots/` + 2 e2e png + `node frontend/resp-audit.cjs` 仍绿。
- **回滚**：`git checkout .benchmarks/ .gitignore`（未 commit 前可还原）。
- **决策边界**：删除属不可逆操作，**需用户授权**（脚本已就绪，授权后执行）。

### 5.3 G3：landing Service Worker（P2，~3h）

- **缺口**：`ls landing/sw.js` → No such file；`frontend`/`landing` 无 registerSW 引用。
- **原子步骤**：见 `计划书/全阶段落地闭环-步骤拆解细则.md` §3（B1-01 ~ B1-14，含 perf 基线 → SW 三策略 Cache-First/Network-First/SWR → 注册 → 离线验证 → API 不被错缓存）。
- **验收**：`cd landing && npm run build` + `node frontend/perf-audit.cjs` LCP 不退化 + 离线可访问静态资源 + `/v1/health` 断网不被错缓存。
- **回滚**：删 `landing/sw.js` + 删 `index.html` 注册脚本 + `cd landing && npm run build`。

### 5.4 G4：智能体 DAG 编排（P3→P1*，~20h 基础 + ~8h v9 深化）

- **缺口**：`api/agent/dag.py`/`planner.py`/`api/routes/agent_dag.py` 全不存在；`rg dag api/` 无残留。
- **基础原子步骤**：见 `计划书/全阶段落地闭环-步骤拆解细则.md` §5（D1-01 ~ D1-33，含前置侦察复用 → DAGNode/DagRun dataclass → TDD 拓扑排序/并行/失败传播 → LLM Planner Mock → 路由端点 + E2E → metrics + 开关）。
- **v9.0.0 深化**（本指南 §6.1 扩展）：基础 DAG 落地后，深化为「多模态融合 + RAG 工具链 + 自反思 critic 循环」，从单线 DAG 升级为可回环的 agentic workflow。
- **验收**：`pytest tests/test_agent_dag.py tests/test_agent_planner.py tests/integration/test_agent_dag_e2e.py -v` 全绿 + `mypy --strict api/agent/dag.py api/agent/planner.py` 0 error + grep planner 无真实付费调用（仅 tryingopen+Mock）。
- **付费红线**：planner 用 tryingopen 免费上游 + `IF_MOCK_UPSTREAM=1` Mock 验证参数拼装，**测试不发起真实付费**。

### 5.5 G5：Cloudflare 免费层接入（P3+L3，~4h 方案，待授权）

- **缺口**：`deploy/docs/cloudflare-cdn.md` 已存在（172 行方案就绪），但域名 NS 迁移属 L3 生产/外部资源，**待用户授权**。
- **方案文档已就绪**：见 `deploy/docs/cloudflare-cdn.md`，含 NS 迁移 + Cache Rules + Rate Limiting + SSE bypass + 回滚。
- **决策边界**：用户确认 `imagefree.tingfengai.art` 可迁移 NS 后方可实施。本指南不擅自改域名 NS。
- **禁区**：SSE 端点需 `Cache-Control: no-cache` + CF bypass 规则，防实时性丢失。

### 5.6 N1：README / SOP / 计划书索引版本滞后（P1，~1-2h，文档漂移）

- **缺口**（全栈分析新发现）：
  - `README.md:9` badge 仍是 `version-8.3.0`（与代码 v8.5.0 不一致）
  - `docs/SOP.md:3` 版本 2.4.0 / 最后更新 2026-09-04，正文仍写"当前代码版本 **v7.7**"（:6）、回滚流程基于 v7.x 标签
  - `docs/release_notes.md` 无本地根文件（`docs/releases/` 目录存在，但根 `release_notes.md` 不存在）——v5-v8.5 无本地记录，仅 git log 可溯
  - `计划书/README.md` 索引仅 1 项且版本停 v7.7.6
- **改动**：小改动单（README badge v8.3.0→v8.5.0 + SOP 版本行 + 补 v8.5.0 发版摘要 + 计划书索引追加本指南 + 4 份上游档案）。
- **验收**：`rg "8\.5\.0" README.md docs/SOP.md 计划书/README.md` 命中 + 计划书索引含本指南 + 4 档案。
- **回滚**：`git checkout README.md docs/SOP.md 计划书/README.md`。

### 5.7 N3：db/queries.py 804 行微超 800（P2，~1h）

- **缺口**（全栈分析新发现）：`wc -l api/db/queries.py` = 804，仅超 4 行，基线档案大文件清单未列（其统计截至 v8.5.0 拆分后）。
- **改动**：微拆（把 `task_to_public` 或某组小工具迁出 `queries_extra.py`）或 800 上限豁免均可，低风险。
- **验收**：`wc -l api/db/queries.py` ≤800 或豁免注释 + `pytest tests/test_db_*.py` 全绿。

### 5.8 N2：data/ 测试残留 DB 膨胀（P2，~1h，本地清理）

- **缺口**（全栈分析新发现）：`data/` 88 个 `*.db`，总 65MB；其中 `queue.db` **29M**、`imagefree.db` 2M，其余大量 `test-*`/`e2e*`/`probe*` 命名测试残留。
- **归属**：`data/*.db` 已被 `.gitignore:26` 覆盖，`git ls-files data/` 仅 8 项（1 png+1 bak+2 shm/wal+4 ui png），**测试残留未被跟踪**（不污染仓库，但占本地磁盘）。
- **改动**：定期清理本地测试残留 DB（`data/test-*`/`e2e*`/`probe*`/`inproc*`/`strict*`/`trace*`），`queue.db` 按保留策略处理（可能含已完成任务记录，需确认保留策略）。
- **决策边界**：删除本地 DB 属不可逆操作，**需用户授权**；不删 git 跟踪的 8 项。

### 5.9 N4：README 外部链接有效性存疑（P3，~0.5h）

- **缺口**（全栈分析新发现）：README 含 `https://imagefree.tingfengai.art` 线上演示链接，但记忆索引表明"服务器已过期/无远程部署"——链接有效性存疑。
- **改动**：标注"演示链接可能不可用"或移除；待用户确认服务器状态后定。

### 5.10 N5：landing/前端 e2e 脚本无统一入口（P3，~1h）

- **缺口**（全栈分析新发现）：`frontend/e2e-smoke.cjs`/`e2e-m4.cjs`/`e2e-p3.cjs`/`e2e_real_fullstack.cjs` + `resp-audit.cjs`/`a11y-audit.cjs`/`perf-audit.cjs` 已存在但 npm scripts 未暴露（package.json 仅 smoke）；自动化审计脚本无统一入口。
- **改动**：`frontend/package.json` scripts 追加 `audit:a11y`/`audit:perf`/`audit:resp`/`audit:all`，统一入口。
- **验收**：`cd frontend && npm run audit:all` 串联三审计。

---

## 6. v9.0.0 核心跃迁（新能力，复用现有轮子深化）

> 本节是 v8.5.0 → v9.0.0 的**主打跃迁**。每项均标注复用的现有轮子位置，不造轮子。基础缺口（G1-G5）收尾后进入本节。

### 6.1 智能体 DAG 编排深化（G4-Plus，P1*，~28h）

**定位**：v9.0.0 主打。基础 DAG（G4，拓扑+并行+失败传播）落地后，深化为「多模态融合 + RAG 工具链 + 自反思 critic 循环」的 agentic workflow。

**复用轮子**（不造轮子）：
- `api/agent/intent.py`（意图分类）→ DAG planner 任务分解入口
- `api/agent/critic.py`（LLM 审查）→ DAG 节点终检 + 自反思循环
- `api/agent/memory.py`（L0-L3 记忆巩固）→ DAG run 记忆 + 跨 run 经验沉淀
- `api/vector/{embed,store}.py`（向量检索）→ RAG 工具节点
- `api/sse_events.py` + `api/ws_events.py` → DAG 节点状态实时推送
- `api/db/queue_store.py`（持久化队列）→ DAG run 持久化
- `api/providers/registry.py`（`all_chat_models`）→ planner 取 tryingopen 免费模型
- `api/prompts/`（compose_system_text）→ planner system prompt
- `api/background.py`（spawn）→ DAG 执行器后台运行

**基础层（G4，见 §5.4 上游档案）**：
1. `api/agent/dag.py`：`DAGNode`/`DagRun` dataclass（frozen）+ `DagEngine`（topo_sort + 并行 `asyncio.gather` + 失败传播 + 状态机 pending→running→success/failed/skipped）
2. `api/agent/planner.py`：LLM 规划（复用 intent.py Mock 模式 + registry + tryingopen；输出 JSON→DagRun）
3. `api/routes/agent_dag.py`：`POST /v1/agents/run`（提交 DAG，异步）+ `GET /v1/agents/run/{id}`（查状态）

**深化层（v9.0.0 新增，基础层落地后）**：

| 子项 | 目标 | 复用/新建 | 验收 |
|---|---|---|---|
| 6.1-a 多模态融合节点 | DAG 节点支持 txt2img→img2img→img2vid 链路 | 新建 `api/agent/tools/multimodal.py`，复用 `providers/{imagefree,nanobanana,falai}.py` | `test_dag_multimodal_chain.py` 绿 |
| 6.1-b RAG 工具节点 | DAG 节点可调向量检索做上下文增强 | 新建 `api/agent/tools/retrieval.py`，复用 `api/vector/store.py` | `test_dag_rag_node.py` 绿 |
| 6.1-c 自反思 critic 循环 | DAG 节点失败后，critic LLM 审查→决定重试/改参/跳过 | 复用 `api/agent/critic.py`，DagEngine 加 `reflect_on_failure` 钩子 | `test_dag_self_reflection.py` 绿 |
| 6.1-d 跨 run 记忆 | DAG run 完成后，经验沉淀到 memory L2/L3 | 复用 `api/agent/memory.py` observe + consolidation | `test_dag_memory_persistence.py` 绿 |
| 6.1-e 条件分支 | DAG 节点支持条件跳转（if-then-else） | DagNode 加 `condition: str \| None`（表达式求值） | `test_dag_conditional_branch.py` 绿 |
| 6.1-f 人机协作 | 长时 DAG 中途可暂停等待人工输入 | 复用 `ws_events.py` 双向通道 + 新增 `human_input` 节点状态 | `test_dag_human_in_loop.py` 绿 |

**禁区**：
- 不得改现有 agent 模块（intent/critic/memory/guard/routes）—— 只追加 `dag.py`/`planner.py`/`tools/`
- 不得发起真实付费 LLM —— planner 用 tryingopen 免费上游 + `IF_MOCK_UPSTREAM=1` Mock
- 不得破坏 SSE/WS 现有端点 —— DAG 状态推送复用 publish/broadcast
- 不得改 `api/agent/__init__.py` 现有导出 —— 只追加 dag/planner 导出

**TDD 清单**（基础层见 §5.4，深化层追加）：
1. RED：`tests/test_dag_multimodal_chain.py::test_txt2img_to_img2img_chain` —— 验证多模态节点串联
2. RED：`tests/test_dag_rag_node.py::test_rag_retrieves_before_generate` —— 验证 RAG 工具调向量检索
3. RED：`tests/test_dag_self_reflection.py::test_critic_loop_retries_on_failure` —— 验证自反思重试
4. RED：`tests/test_dag_memory_persistence.py::test_run_experience_consolidates` —— 验证跨 run 记忆
5. GREEN：实现各深化子项
6. mypy strict：`mypy --strict api/agent/dag.py api/agent/planner.py api/agent/tools/*.py` 0 error

**验收**：
```bash
pytest tests/test_agent_dag*.py tests/test_dag_*.py tests/integration/test_agent_dag_e2e.py -v
ruff check api/agent/dag.py api/agent/planner.py api/agent/tools/ api/routes/agent_dag.py
mypy --strict api/agent/dag.py api/agent/planner.py
rg "fal.ai|imagefree.*真实付费" api/agent/planner.py  # 无真实付费
```

**回滚**：删 `api/agent/dag.py`/`planner.py`/`tools/` + 还原 `api/routes/__init__.py` + `api/agent/__init__.py`。

### 6.2 MCP 协议化（agent 工具 → MCP server，P2，~12h）

**定位**：agent 工具调用从私有协议升级到 MCP（Model Context Protocol），对外可被发现/可组合，让听风AI 的工具能力可被 Claude Code / Cursor / Continue 等 MCP 客户端直接消费。

**复用轮子**：
- `api/skills/`（skills 四件套）→ 直接暴露为 MCP tools
- `api/agent/guard.py`（PreToolUse 硬门禁）→ MCP 工具调用前置风控
- `api/providers/registry.py` → MCP 工具的 provider 路由
- `api/auth.py`（`IF_API_KEYS` + `IF_ADMIN_KEYS`）→ MCP 鉴权

**改动**（L2，双轨并存）：
1. 新建 `api/mcp/` 包：`server.py`（MCP server 实现）+ `tools.py`（工具注册，把 `api/skills/` 暴露为 MCP tools）+ `transport.py`（stdio/SSE 双传输）
2. `api/routes/mcp.py`：`POST /v1/mcp/sse`（SSE 传输）+ `GET /v1/mcp/tools`（工具清单）
3. 双轨并存：现有 `/v1/agent/*` 端点保留，MCP 是并行对外通道；`IF_MCP_ENABLED=0` 缺省关闭
4. 鉴权：MCP 客户端用 `IF_API_KEYS`（Bearer），工具调用走 `guard_chat_request`

**禁区**：
- 不得破坏现有 `/v1/agent/*` 端点契约
- 不得改 `api/skills/` 现有 SKILL.md 结构
- MCP 工具调用仍走付费红线（Mock 验证 + tryingopen 免费上游）

**TDD 清单**：
1. RED：`tests/test_mcp_server.py::test_list_tools_returns_skills` —— 验证 skills 暴露为 MCP tools
2. RED：`tests/test_mcp_server.py::test_call_tool_routes_to_provider` —— 验证工具调用路由
3. RED：`tests/test_mcp_server.py::test_guard_blocks_unauthorized_tool` —— 验证 guard 前置风控
4. RED：`tests/integration/test_mcp_e2e.py::test_sse_transport_end_to_end` —— SSE 传输端到端
5. GREEN：实现 `api/mcp/`
6. mypy strict：`mypy --strict api/mcp/server.py` 0 error

**验收**：
```bash
pytest tests/test_mcp_*.py tests/integration/test_mcp_e2e.py -v
ruff check api/mcp/
# MCP 客户端测试：用 Claude Code 配置 MCP server 指向 /v1/mcp/sse，确认工具可见
```

**回滚**：`IF_MCP_ENABLED=0` 即关闭 + 删 `api/mcp/` + 还原路由。

### 6.3 RAG 知识库增强（复用 vector/，P2，~10h）

**定位**：把 v8.5.0 已落地的向量检索（`api/vector/`）深化为 RAG 知识库，让 agent/chat 在生成前检索历史任务/prompt 库/FAQ，提升输出质量。

**复用轮子**：
- `api/vector/{embed,store}.py`（SimHash + sqlite-vec）→ 向量存储已就绪
- `api/db/queries.py`（任务查询）→ 历史任务检索
- `api/prompts/`（compose_system_text）→ RAG context 注入 system prompt

**改动**（L2）：
1. 新建 `api/rag/` 包：`indexer.py`（任务完成时计算 embedding 入库）+ `retriever.py`（query 时检索 top-K）+ `injector.py`（把检索结果注入 system prompt）
2. `api/routes/chat.py`：`POST /v1/chat/completions` 前置 RAG 检索（`IF_RAG_ENABLED=0` 缺省关）
3. `api/routes/generate.py`：`POST /v1/generate` 前置 prompt 相似检索（避免重复生成 + 热门 prompt 复用缓存图）

**禁区**：
- 不得改 `api/vector/` 现有接口 —— 只追加 `api/rag/` 消费层
- RAG 检索不发起真实付费 —— embed 用本地 SimHash（已就绪），不调外部 embedding API

**TDD 清单**：
1. RED：`tests/test_rag_indexer.py::test_index_task_on_completion` —— 验证任务完成触发索引
2. RED：`tests/test_rag_retriever.py::test_retrieve_top_k_similar` —— 验证 top-K 检索
3. RED：`tests/test_rag_injector.py::test_inject_context_to_system_prompt` —— 验证 context 注入
4. GREEN：实现 `api/rag/`
5. mypy strict：`mypy --strict api/rag/` 0 error

**验收**：
```bash
pytest tests/test_rag_*.py -v
ruff check api/rag/
# RAG 开启后 chat 输出质量对比（人工评估或 LLM-as-judge）
```

**回滚**：`IF_RAG_ENABLED=0` 即关闭 + 删 `api/rag/`。

### 6.4 流式工具调用协议增强（P2，~8h）

**定位**：`routes/chat.py` 现状"tools 仅转发，无本地执行回路"（anatomy.md 标注），v9.0.0 补本地工具执行回路，让 function calling 真正在本机执行（复用 MCP 工具）。

**复用轮子**：
- `api/mcp/tools.py`（§6.2）→ 工具执行直接调 MCP tools
- `api/providers/tryingopen/`（chat_tools 能力）→ 上游工具调用
- `api/sse_events.py` → 工具执行状态流式推送

**改动**（L2）：
1. `api/routes/chat.py`：补 `tool_calls` 本地执行回路（接收上游 tool_calls → 调 MCP tools 执行 → 把结果回传上游 → 继续生成）
2. `IF_TOOL_EXECUTION_ENABLED=0` 缺省关闭（默认仅转发，安全）

**禁区**：
- 不得破坏现有 `/v1/chat/completions` 流式契约
- 工具执行走 `guard` 前置风控 + 付费红线

**TDD 清单**：
1. RED：`tests/test_chat_tool_loop.py::test_tool_call_executes_locally` —— 验证本地执行
2. RED：`tests/test_chat_tool_loop.py::test_tool_result_returns_to_provider` —— 验证回传续生成
3. GREEN：实现 tool loop
4. mypy strict：`mypy --strict api/routes/chat.py`（可选，chat.py 较大）

**验收**：
```bash
pytest tests/test_chat_tool_loop.py -v
# 真实测试：tryingopen tool_calls → 本地 MCP 执行 → 续生成（最小真实调用）
```

**回滚**：`IF_TOOL_EXECUTION_ENABLED=0` 即回退仅转发。

### 6.5 成本智能调度（复用 cost_forecast + adaptive_router，P3，~6h）

**定位**：把 v8.5.0 已落地的成本预测（`cost_forecast.py`）接入路由引擎（`adaptive_router.py`），让路由打分纳入成本维度。

**复用轮子**：
- `api/cost_forecast.py`（`predict_budget_burn`）→ 成本预测已就绪
- `api/adaptive_router.py`（MAB-EWMA + Thompson/UCB1）→ 路由打分引擎已就绪

**改动**（L2，只调参 + 接线）：
1. `api/adaptive_router.py`：`select_provider()` 打分公式加成本维度 —— `Score = (成功率/log10时延) × 负载惩罚 × 成本惩罚`（成本高的 provider 在预算紧张时降权）
2. `IF_COST_AWARE_ROUTING=0` 缺省关闭（默认纯成功率/时延打分）

**禁区**：
- 不得改 `adaptive_router` 的 Thompson/UCB1 算法核心 —— 只在打分公式追加维度
- 不得改 `cost_forecast.py` 接口 —— 只消费其输出

**TDD 清单**：
1. RED：`tests/test_cost_aware_routing.py::test_high_cost_provider_deprioritized` —— 验证成本降权
2. RED：`tests/test_cost_aware_routing.py::test_budget_warning_triggers_throttle` —— 验证预算告警触发降权
3. GREEN：接线

**验收**：
```bash
pytest tests/test_cost_aware_routing.py tests/test_adaptive_router*.py -v
```

**回滚**：`IF_COST_AWARE_ROUTING=0` 即回退纯成功率/时延打分。

---

## 7. 架构深化（不重构，只接线/扩面）

> 本节均遵循"只改进不重构"铁律：不改公共接口签名、不改 DB schema、旧文件留兼容垫片。

### 7.1 storage Redis 集群化与多实例共享（复用 redis_adapter，P2，~6h）

**现状**：v8.3.0 已接 storage 热路径（`request_guard.py:260,530` 真调 `adapter.rate_limiter.is_allowed`），单 Redis 实例。

**深化**：
1. `api/storage/redis_adapter.py`：补 Redis Sentinel/Cluster 模式（`IF_REDIS_MODE=sentinel|cluster`，缺省 `standalone`）
2. `deploy/docker-compose.yml` redis profile：补 sentinel/cluster 编排（`profiles: ["redis-cluster"]`）
3. 降级语义保持：Redis 不可用降级内存桶（不 fail-open，v8.3.0 已立）

**验收**：`pytest tests/test_request_guard_redis_mode.py` + 新建 `test_redis_cluster_mode.py` 绿。

### 7.2 队列优先级智能调度（复用 PriorityQueue，P3，~4h）

**现状**：`api/worker/queue.py` CountedPriorityQueue 已就绪。

**深化**：
1. 优先级抢占：admin 任务（priority=0）可抢占普通任务（已有 priority=0 机制，深化为"抢占式"——worker 空闲时优先取 priority=0）
2. 公平调度：同优先级内 FIFO + 加权轮询（避免某用户饥饿）
3. `IF_QUEUE_FAIR_SCHEDULING=0` 缺省关

### 7.3 worker 池弹性伸缩增强（复用 scaler.py，P3，~4h）

**现状**：`api/worker/scaler.py`(261) 多维扩缩容评分已落地（v8.5.0 P0-F4 拆出）。

**深化**：
1. 预测式扩容：基于历史 RPS + 当前队列深度预测未来 N 秒负载，提前扩容（复用 `cost_forecast.py` 的预测模式）
2. 上游健康感知：某 provider 熔断时，缩减专门服务该 provider 的 worker（若可区分）
3. `IF_WORKER_PREDICTIVE_SCALING=0` 缺省关

### 7.4 SSE/WS 统一实时总线（复用 sse_events + ws_events，P3，~6h）

**现状**：SSE（`sse_events.py`）+ WS（`ws_events.py`）双通道并存。

**深化**：
1. 统一事件总线：新建 `api/events/bus.py`，SSE/WS 都从同一 bus 订阅（避免重复 publish）
2. 事件回放统一：Last-Event-ID（SSE）+ WS reconnect 都走 bus 的 replay
3. 不破坏现有端点 —— bus 是内部重构，对外接口不变

### 7.5 agent 记忆巩固 worker 增强（复用 memory.py，P3，~4h）

**现状**：`api/agent/memory.py` L0-L3 记忆巩固 worker（lifespan 启动）已落地。

**深化**：
1. 巩固策略可配：`IF_MEMORY_CONSOLIDATION_STRATEGY=llm|heuristic|hybrid`（缺省 hybrid）
2. 记忆 TTL：L0 短期记忆自动过期（避免无限增长）
3. 记忆审计：`GET /v1/agent/memory?level=L2` 支持按级别查询 + 清理

### 7.6 adaptive_router 拆分（接近 800 上限，P2，~4h）

**现状**：`adaptive_router.py`(770) 接近 800 行上限。

**改动**（参照 email_pool/account_pool 拆分先例）：
1. 拆出 `adaptive_router/score.py`（打分公式）+ `persistence.py`（SQLite 持久化 + warm）+ `exploration.py`（Thompson/UCB1 探索）
2. `adaptive_router.py` 留兼容垫片 `from .adaptive_router import *`（re-export）
3. 注意 from-import 值拷贝陷阱（`.wolf/cerebrum.md` Do-Not-Repeat）—— monkeypatch 用 `_pkg_attr()` 运行时读包命名空间

**TDD**：复用 `tests/test_adaptive_router*.py` + 新建 `test_adaptive_router_compat.py`（旧 import 路径可用）+ `test_async_sync_contamination.py`（必跑）。

---

## 8. 性能登顶

### 8.1 cf_solver 多节点联邦扩容（G-ops，P1，~1 天，本地验证）

**现状**：`api/solver_guard.py` 已支持多节点联邦 + 熔断 + IdleTimeout（代码层就绪），`deploy/docker-compose.yml` 单 `cf_solver` 服务。

**改动**（仅调参 + 配置，不改逻辑）：
1. `deploy/docker-compose.yml`：`cf_solver` 服务加 `deploy.replicas: 3` 或用 `--scale cf_solver=3`
2. `deploy/.env`：`IF_CF_SOLVER_URL=http://cf_solver:8001`（Docker 内部 LB 自动轮询 replicas）或逗号分隔多实例
3. `api/solver_guard.py`：调参（熔断阈值 / 节点健康检查间隔 / 负载均衡策略）—— **只调参不改逻辑**

**验收**：
```bash
cd deploy && docker compose up -d --scale cf_solver=3
curl -s http://localhost:8100/v1/health | grep -i solver  # 联邦健康
pytest tests/test_solver_guard*.py tests/test_token_pool.py -v
python scripts/loadtest.py  # token 水位 ≈ 3× 单节点
```

**回滚**：`cd deploy && docker compose up -d`（单副本）+ `IF_CF_SOLVER_URL` 回单节点。
**禁区**：生产扩容属 L3，需用户授权（本计划只编排本地 `--scale` 验证，不擅自部署生产）。

### 8.2 landing LCP<1.5s + Service Worker（G3 收尾，P2，~3h）

见 §5.3 G3。基础 SW 落地后，深化：
1. 关键 CSS inline + 字体 preload（`landing/index.html` 已有 preload base.css，深化为关键 CSS inline）
2. Three.js 粒子数断点细化（`Hero3D.vue`）
3. motion-v 改 IntersectionObserver 原生 + CSS（减 JS 包）
4. 图片 AVIF/WebP + `loading=lazy` + 显式 width/height

### 8.3 前端 bundle 瘦身 + 代码分割（P2，~6h）

**现状**：React 19 + recharts + react-router-dom 7，bundle 体积待实测（执行方跑 `npm run build` 看 dist 体积）。

**改动**：
1. `frontend/vite.config.ts`：`build.rollupOptions.output.manualChunks` 拆分 recharts/react-router/three 等大依赖
2. 路由级懒加载：`React.lazy(() => import('./pages/XXX'))` + Suspense（已有 Skeleton 兜底）
3. recharts 按需引入（避免全量）
4. three.js 动态 import（landing 已做，frontend 若用也照此）

**验收**：
```bash
cd frontend && npm run build
# dist/assets/*.js gzipped < 300kb（app page 预算）
node frontend/perf-audit.cjs  # LCP/INP/CLS 达标
```

### 8.4 SQLite 读写分离 + WAL 优化（P3，~4h）

**现状**：`api/db/core.py`(371) WAL + 连接池 + 0.2s 批量写 + `isolation_level=None` autocommit（Do-Not-Repeat 已立）。

**深化**（不重构，只调参）：
1. 读连接池扩容（`IF_DB_READ_POOL_SIZE`，缺省与写池一致）
2. WAL checkpoint 策略调参（`IF_DB_WAL_CHECKPOINT_INTERVAL`）
3. 查询索引审计：`EXPLAIN QUERY PLAN` 跑高频查询，补缺失索引
4. **不换 Postgres**（触发器未出现前是负优化，见 §2.7）

### 8.5 代理池智能调度（复用 proxy_pool，P3，~4h）

**现状**：`api/proxy_pool.py`(386) 住宅代理 + 免费代理双源 + 健康评分 + 粘滞（v7.7.15 已落地）。

**深化**：
1. 上游感知粘滞：某 provider 对某代理 IP 成功率高时，优先复用（provider×proxy 二维评分）
2. 代理预热：低峰期预热新代理（健康检查 + Turnstile 试解）
3. `IF_PROXY_PROVIDER_AFFINITY=0` 缺省关

---

## 9. 安全加固

### 9.1 Cloudflare WAF + 边缘限流（G5，L3 待授权，~4h 方案）

见 §5.5 G5。方案文档已就绪（`deploy/docs/cloudflare-cdn.md`），待用户授权域名 NS 迁移后实施。

### 9.2 CSP nonce 化 + 安全头增强（P2，~4h）

**现状**：`api/main.py SecurityHeadersMiddleware` 已就绪（HSTS/XCTO/XFO/RRP + 可选 CSP），CSP 当前可能是 `unsafe-inline` 或未启用。

**改动**：
1. CSP nonce 化：每请求生成随机 nonce，`Content-Security-P: script-src 'self' 'nonce-{RANDOM}'`（替换 `unsafe-inline`）
2. `api/main.py`：SecurityHeadersMiddleware 扩常量 + nonce 注入
3. 前端：所有 inline script 加 `nonce` 属性（若有）
4. 补 `Permissions-Policy`（`camera=(), microphone=(), geolocation=()`）

**验收**：
```bash
curl -sI http://localhost:8100/ | grep -i "content-security-policy\|permissions-policy"
# CSP 含 nonce，无 unsafe-inline
```

### 9.3 密钥轮换自动化（P2，~4h）

**现状**：密钥仅经 `IF_*` 环境变量注入，无轮换机制。

**改动**：
1. `api/auth.py`：`IF_API_KEYS`/`IF_ADMIN_KEYS` 支持热重载（文件 mtime 监听 or 定时重读）
2. 双密钥重叠期：新密钥生效后，旧密钥保留 N 小时再失效（平滑过渡）
3. `scripts/rotate_keys.py`：轮换脚本（生成新密钥 → 写 env → 触发热重载 → N 小时后清旧密钥）

### 9.4 审计日志全链路（复用 audit.py，P3，~4h）

**现状**：`api/audit.py` trace_id 透传可 grep 串联已就绪。

**深化**：
1. DAG run 全链路审计：每个节点的 tool/provider/params/result 入审计
2. 审计日志查询：`GET /v1/audit/run/{id}` 按 run 查全链路
3. 审计日志保留策略：14 天落盘（`disk_logger.py` 已有）+ 长期归档（可选）

### 9.5 反滥用 AI 识别（P3，~6h）

**现状**：反滥用靠 `request_guard.py` per-IP 限流 + `engine._finish` task-failure-burst 自动封禁（v7.7.4）。

**深化**：
1. 异常模式识别：基于请求频率/prompt 模式/失败率，用 agent/intent.py 识别滥用意图
2. 渐进式封禁：第一次警告 → 限流降级 → 封禁（而非直接封禁）
3. 申诉渠道：封禁 403 附 `IF_ADMIN_CONTACT`（已有），深化为申诉表单

### 9.6 SBOM / SLSA 供应链安全（P3，~4h）

**现状**：`requirements.txt` 声明依赖，无 SBOM。

**改动**：
1. `pip-audit` 集成 CI：`.github/workflows/ci.yml` 加 `pip-audit` step
2. SBOM 生成：`cyclonedx-py` 生成 `sbom.json`（每次发版）
3. SLSA provenance：GitHub Actions 生成 provenance（SLSA Level 3）

---

## 10. UI/UX 登顶

> 对照 `rules/web/design-quality.md` 的"反模板政策"，从"登顶组件"升级到"设计系统"。

### 10.1 设计系统统一（design tokens + compound components，P2，~16h）

**现状**：v8.5.0 已落地 Skeleton/EmptyState/CommandPalette + useOptimisticMutation/useVirtualList，但无统一设计系统。

**改动**（L2，参照 `rules/web/coding-style.md`）：
1. design tokens：`frontend/src/styles/tokens.css` 定义 `--color-*`/`--text-*`/`--space-*`/`--duration-*`（用 oklch 色彩空间）
2. compound components：`Tabs`/`Dialog`/`Popover` 用 compound 模式（`<Tabs.List><Tabs.Trigger><Tabs.Content>`）
3. 主题：暗色模式 + 主题切换（`data-theme` 属性 + CSS variables）
4. 排版：中文文案排版指北（中英文空格、全半角标点）—— 复用 `chinese-documentation` skill

**禁区**：不重构现有 13 页，只把重复样式提取到 tokens。

### 10.2 管理面板 13 页体验深化（P2，~12h）

**现状**：v8.5.0 已落地登顶组件（骨架/空态/命令面板/乐观更新/虚拟列表）。

**深化**（每页独立，可并行）：

| 页面 | v9.0.0 深化 | 复用 |
|---|---|---|
| Dashboard | 实时流式指标（SSE/WS 驱动）+ 预测曲线 | `sse_events.py` + `cost_forecast.py` |
| Tasks | 虚拟列表 + 实时进度条 + SSE/WS 状态推送（已有，深化为可中断） | `useVirtualList` + `ws_events.py` |
| Generate | 乐观提交 + 进度条 + 失败重试 + 历史相似图推荐（RAG） | `useOptimisticMutation` + `api/rag/` |
| ChatPlayground | 工具调用可视化 + Vision 图片粘贴 + 思考链折叠 + RAG 引用展示 | `api/mcp/` + `api/rag/` |
| Accounts | 余额预测曲线 + FSM 状态机可视化 | `cost_forecast.py` + `account_pool/fsm.py` |
| Providers | 健康评分 + 历史时延分位图 + 成本对比 | `adaptive_router` 持久化 |
| Health | solver 联邦节点拓扑图 + 熔断事件流 | `solver_guard.py` |
| Logs | 虚拟列表 + 级别过滤 + 全文搜索 + 导出 | `useVirtualList` + `log_buffer.py` |
| Security | 封禁原因展示 + 申诉联系方式 + IP 地图 | `request_guard.py` |
| Slow | 分阶段耗时火焰图 | `slow_log.py` |
| DLQ | 批量重试 + 失败原因分类 | `worker/dlq.py` |
| Costs | 预测曲线 + 预算警戒线 + 成本智能调度可视化 | `cost_forecast.py` + §6.5 |
| Ecosystem | 提供商能力矩阵 + 接入引导 + DAG 模板市场 | `providers/registry.py` + §6.1 |

**交互反馈检查点**（执行方浏览器验证时重点观察）：
- 点击后是否有反馈（按钮 loading/disabled）
- 加载状态是否明确（骨架屏 vs spinner）
- 错误文案是否可行动
- 空状态是否提供下一步
- 返回/刷新后数据是否丢失
- 慢请求期间是否可误操作
- 键盘导航是否可用
- 移动端是否溢出/遮挡

### 10.3 暗色模式 + 主题切换（P2，~4h）

**现状**：无暗色模式。

**改动**：design tokens（§10.1）定义双套色，`data-theme="dark"` 属性切换，localStorage 持久化，`prefers-color-scheme` 自动跟随。

### 10.4 a11y WCAG 2.2 AA 全量（复用 a11y-audit.cjs，P2，~4h）

**现状**：v8.5.0 已落地 a11y-audit.cjs（0 critical）。

**深化**：
1. 页面级补全：每页 input `aria-label` / 表单 `aria-invalid` / 动态区域 `aria-live` / 图标按钮 `aria-label`
2. WCAG 2.2 新增：Focus Appearance（焦点可见性）+ Target Size Minimum（44px 触摸目标）
3. 键盘导航：所有交互可达 + focus trap（Dialog/CommandPalette）

### 10.5 国际化 i18n（P3，~8h）

**现状**：landing 中英双语（v7.2），管理面板中文。

**改动**：
1. `frontend/src/i18n/`：react-i18next 接入 + `zh.json`/`en.json` 词条
2. 语言切换：`zh-CN`/`en` 两套，localStorage 持久化
3. `IF_DEFAULT_LANG` 环境变量配置默认语言

### 10.6 实时协作（多管理员，P3，~8h）

**现状**：单管理员（`IF_ADMIN_KEYS`）。

**改动**（预留多租户路径）：
1. `api/auth.py`：多管理员 Key + 各自 session
2. `api/ws_events.py`：管理员操作广播（A 改了某配置，B 的面板实时刷新）
3. CRDT/OT：实时编辑冲突解决（轻量级，用 Yjs 或自实现）

---

## 11. 运维与可观测登顶

### 11.1 litestream 异地秒级备份（已落地，深化恢复演练，P3，~4h）

**现状**：v8.5.0 已落地 `deploy/litestream.yml`（R2/S3 占位凭证）。

**深化**：
1. 恢复演练常态化：`scripts/restore_drill.py` 每周自动跑 restore 演练 + 数据一致性校验
2. `deploy/docs/litestream-restore.md` 已有 SOP，补演练记录到 `docs/verification-log.md`

### 11.2 Grafana 仪表盘深化（已落地，加 DAG/SLO 仪表盘，P3，~4h）

**现状**：v8.5.0 已落地 `deploy/grafana/dashboards/{imagefree-overview,slo-budget}.json` + provisioning。

**深化**：
1. `deploy/grafana/dashboards/agent-dag.json`：DAG run 数 + 节点状态分布 + 自反思循环次数 + RAG 命中率
2. `deploy/grafana/dashboards/cost.json`：成本消耗 + 预算燃烧 + 预测超预算日期
3. 告警规则：SLO 错误预算消耗 >50% 告警 + DAG 失败率 >阈值告警

### 11.3 UptimeRobot 拨测（已落地，深化告警通道，P3，~2h）

**现状**：v8.5.0 已落地 `deploy/docs/monitoring.md`（UptimeRobot 5min 拨测 + 邮件告警）。

**深化**：
1. 多通道告警：邮件 + 企业微信 webhook + Telegram bot
2. 拨测端点深化：`/v1/healthz` + `/v1/livez` + `/v1/readyz` + `/v1/agent/health`（DAG 落地后）
3. 告警分级：critical（宕机）/warning（降级）/info（慢）

### 11.4 分布式追踪 OTel 深化（P3，~6h）

**现状**：`api/telemetry.py` OTel tail-based 采样（错误 100% + 正常 10%）已就绪。

**深化**：
1. DAG run 全链路 trace：每个节点一个 span，串联 run_id
2. OTel Collector：`deploy/otelcol.yml` 配置 + Tempo/Jaeger 后端（可选 profile）
3. 跨服务 trace：MCP 调用 + 上游 provider 调用串联

### 11.5 混沌工程常态化（P2，~8h）

**现状**：`tests/chaos/test_fault_injection.py` 已有故障注入测试。

**深化**：
1. 混沌实验目录：`tests/chaos/experiments/` 每个实验一个文件（Redis 断连 / cf_solver 多节点故障 / 上游 5xx / 代理池枯竭 / SQLite 锁）
2. CI 定期跑：`.github/workflows/chaos.yml` 每周定时跑混沌实验
3. Gremlin 式故障注入：`api/chaos/injector.py` 运行时故障注入（`IF_CHAOS_ENABLED=0` 缺省关）

### 11.6 SRE Runbook 自动化（P3，~6h）

**现状**：`docs/SOP.md` 有运维 SOP。

**深化**：
1. `deploy/docs/runbooks/` 目录：每个常见故障一个 runbook（solver 熔断 / 号池空 / 代理池枯竭 / DB 锁 / DLQ 堆积）
2. 自动化诊断：`GET /v1/diagnostics` 已有，深化为按故障类型给 runbook 链接
3. 自动修复脚本：`scripts/remediate_*.py` 常见故障自动修复（如 DLQ 批量重试）

---

## 12. 测试与质量登顶

### 12.1 mypy strict 扩面（G1 + 深化，P0+P2，~8h+8h）

**基础（G1）**：见 §5.1，扩到 4 模块（+`db.core`+`db.queries`）。

**深化**（v9.0.0）：
1. 继续扩到 `api/agent/`（dag.py/planner.py 新模块直接 strict）+ `api/vector/` + `api/storage/` + `api/rag/` + `api/mcp/`
2. `api/routes/` 渐进 strict（chat.py/generate.py 较大，可分批）
3. 目标：v9.5.0 全 `api/` strict 0 error

### 12.2 测试金字塔补全（P2，~12h）

| 层 | 现有 | v9.0.0 目标 |
|---|---|---|
| 单元测试 | 135 文件 / 1857 用例 | +40 用例（DAG/MCP/RAG/设计系统/cost-aware-routing） |
| 集成测试 | 13 文件 | +8 用例（DAG E2E/MCP E2E/RAG E2E/tool loop/chaos experiment） |
| 混沌测试 | 已有 | +5 实验（Redis 断连/cf 多节点故障/上游 5xx/代理枯竭/DB 锁） |
| 前端单测 | 197 用例 | +40 用例（设计系统组件/暗色模式/i18n/实时协作） |
| E2E | 22 断言 | +15 断言（DAG 提交/命令面板/乐观更新/暗色切换/i18n） |
| 响应式 | 20 断言 | 保持（新页面补） |
| 性能 | perf-audit.cjs | +CWV 断言（LCP<1.5/INP<100/CLS<0.1）+ bundle 体积预算 |
| a11y | a11y-audit.cjs | 0 critical（深化 Focus Appearance/Target Size） |
| 契约测试 | AST 契约 | +OpenAPI 契约（新增端点） |

### 12.3 E2E 真实浏览器路径（P2，~6h）

**现状**：`frontend/e2e-smoke.cjs`（22 断言）已落地，但 playwright MCP 本次连接失败未代验。

**深化**：
1. Playwright 真实用户路径：登录 → 各页点击 → 表单提交 → SSE/WS 状态确认 → 暗色切换 → 命令面板 → 错误路径覆盖
2. 视觉回归：关键页面 4 断点（375/768/1024/1440）截图基线（已有 `resp-shots/`，深化为每页）
3. `IF_E2E_BASE` 环境变量指向运行中服务

### 12.4 性能基准回归（P2，~4h）

**现状**：`scripts/loadtest.py` + `tests/performance/` 已有。

**深化**：
1. pytest-benchmark 基线：核心路径（generate 入库 / queue 入队 / db 批量写 / adaptive_router 打分）建立基准
2. 性能回归告警：CI 跑 benchmark，与基线对比，退化 >10% 告警
3. `deploy/grafana/dashboards/perf.json`：性能指标可视化

### 12.5 安全扫描自动化（P2，~4h）

**现状**：无安全扫描 CI。

**改动**：
1. `pip-audit`：CI 加 step，扫描依赖漏洞
2. `semgrep`：CI 加 step，扫描代码安全模式
3. `secret-scan`：CI 加 step，扫描硬编码密钥（gitleaks）
4. SBOM 生成（见 §9.6）

### 12.6 .benchmarks 清理（G2，~1h）

见 §5.2 G2。

### 12.7 测试覆盖率策略（不为 100% 写无断言测试）

- 核心业务和安全规则：场景覆盖完整（正常/边界/失败/权限/并发/重复提交）
- 本次变更代码：高覆盖无关键分支遗漏
- 关键模块（agent/dag/mcp/rag/storage/adaptive_router）：建议 ≥90%
- 全局覆盖率：不低于 80% 基线（CI 门禁）
- 无法通过覆盖率发现的：用集成/E2E/真实路径补足

### 12.8 「验证过勿重跑」结论（继承自 `.wolf/cerebrum.md` + `docs/verification-log.md`，代码未动前长期有效）

以下均 v7.7/v8.0/v8.5 已闭环，**勿当待办重做**：

- threading.Lock 审计（v6.8.0）：8 文件纯内存临界区保留正确
- nanobanana Action ID / tryingopen 目录：动态嗅探+静态兜底自愈
- 同步 sqlite3：account_pool/email_pool 已 aiosqlite 迁移
- AST 契约 `test_async_sync_contamination.py`：改 account_pool/email_pool/nanobanana/aifreeforever/imagefree 后必跑
- geo_ip 在线查询已下放线程池（v7.7）
- fire-and-forget 已统一走 background.spawn（v7.7）
- log_ws 广播已锁内快照+锁外发送（v7.7）
- 幂等 key 日志已脱敏（v7.7）
- compose env_file:.env 已生效（v7.7）
- CI frontend-gate job 已存在（v7.7）
- 集成/混沌已分轮跑（v7.7）
- 幂等 claim_idempotency 已原子化（v7.6）
- _PROVIDER_TASKS drain 已入 lifespan ③.5（v7.6）
- websockets<14 + WS OPEN OK（v7.7.10）
- index.html no-cache 根治 vite 新 chunk 卡死（v7.7.11）
- 实时日志 /v1/logs + /v1/logs/ws 公益只读开放（v7.7.8）
- 反滥用自动封禁（v7.7.4）
- CI 集成轮不卡覆盖率（v7.7.6）
- **adaptive_router Thompson/UCB1 已落地（v8.0 P1-3）** —— 勿当待办重做
- **ws_events.py WebSocket 双向通道已落地（v7.7.17 P1-6）** —— 勿当待办重做
- **agent 子系统已落地 + E2E + 指标（v8.1+v8.5 P1-A）** —— 勿重写，只深化
- **ruff select 规则族已引入 0 error（v7.7.15 P0-9）** —— 勿当待办重做
- **持久化队列默认开启（v7.7.13 P1-8）** —— 勿当待办重做
- **代理池健康评分+粘滞已落地（v7.7.15 P1-4）** —— 勿当待办重做
- **storage 热路径真接线（v8.3.0 P0-S1）** —— 勿当待办重做
- **大文件拆分收尾（v8.5.0 P0-F1~F4）** —— 勿当待办重做
- **litestream + Grafana + UptimeRobot + compose profiles（v8.5.0 P1-O）** —— 勿当待办重做
- **前端登顶组件 + a11y/perf 审计（v8.5.0 P2-C）** —— 勿重写，只深化
- **向量检索 + 成本预测（v8.5.0 P3-D）** —— 勿当待办重做
- **flaky test_account_growth 已根治（v8.5.0 P2-F1，8s 最终一致性轮询）** —— 勿当待办重做

---

## 13. 成本与商业化

### 13.1 成本预测增强（复用 cost_forecast，P3，~4h）

**现状**：v8.5.0 已落地 `cost_forecast.py`（`predict_budget_burn`）+ `Costs.tsx` 预算燃烧预测 + 全屏预警横幅。

**深化**：
1. 多 provider 成本对比：`GET /v1/cost/providers` 按 provider 拆分成本
2. 成本异常检测：某 provider 成本突增告警
3. 成本优化建议：基于历史数据推荐最优 provider 组合

### 13.2 配额计费雏形（P3，~8h）

**现状**：公益开放不限 Key，无计费。

**改动**（预留商业化路径，不破坏公益开放）：
1. `api/quota/` 包：per-API-key 配额（`IF_QUOTA_ENABLED=0` 缺省关，公益模式无配额）
2. `IF_API_KEYS` 配置的 key 可有配额（`IF_KEY_QUOTA_{KEY}=N`）
3. 配额查询：`GET /v1/quota/usage`（按 key 查用量/剩余）
4. 超配额 429 + Retry-After

### 13.3 多租户隔离预留（P3，~8h）

**现状**：单租户（站长 + 公益访客）。

**改动**（只预留，不实施）：
1. `api/tenant/` 包：tenant_id 字段预留（DB schema 加 `tenant_id` 列，缺省 'default'）
2. `IF_MULTI_TENANT=0` 缺省关（单租户模式零影响）
3. 多租户启用时：per-tenant 号池/代理池/配额隔离

---

## 14. 产品与数据逻辑

| 项 | 现状 | v9.0.0 改进 |
|---|---|---|
| 数据一致性 | SQLite WAL + 批量写 0.2s 窗口 + autocommit | 不动（富余） |
| 状态机完整性 | account_pool FSM（unregistered→registering→active→working→cooling→dead） | cooling 自愈路径增强（可配重试+指数退避） |
| 幂等性 | `claim_idempotency` 原子化（ON CONFLICT DO NOTHING + rowcount） | DAG run 幂等（相同 run_id 重提交不重复执行） |
| 并发/竞态 | per-IP 分片锁 + 借还互斥 + storage Redis 分布式锁 | DAG 节点互斥（同 task 的 img2img 节点不并发） |
| 事务边界 | aiosqlite 批量写 | 不动 |
| 缓存一致性 | LRU 内存缓存 | 内存预算化（`IF_CACHE_MAX_MEMORY`） |
| 重试/超时 | `retry_policy.py` + mypy strict | DAG 节点级重试策略（可配 per-node） |
| 重复提交 | 幂等 claim | DAG run 幂等 + prompt 相似去重（RAG） |
| 分页/排序 | `dispatch.py` 路由记录 | 游标分页（大列表性能） |
| 输入校验 | `routes/generate.py` `_prepare` 校验 priority=0 admin | DAG 节点参数 schema 校验（Pydantic） |
| 数据迁移 | `db/migrations.py` 已独立 | DAG run 表迁移 + 向量索引迁移 |
| 失败补偿 | DLQ + 熔断 | DAG 节点失败补偿（自反思重试 + 跳过 + 降级） |

---

## 15. 性能、稳定性和长期运行

| 项 | 现状 | 是否真瓶颈 | v9.0.0 改进 |
|---|---|---|---|
| API 吞吐 | uvicorn 单进程 asyncio，worker 4-16 | 否（上游 6-30s/张） | 不动 |
| DB 写入 | SQLite WAL + 批量 0.2s | 否（<10 写/s） | 读写分离 + 索引审计（§8.4） |
| Token 获取 | cf_solver 6.13s/次（单节点） | **是，头号瓶颈** | §8.1 多节点联邦扩容 |
| 静态资源 | FastAPI StaticFiles 直出 | 边缘场景 | §5.5 Cloudflare CDN（L3） |
| 内存 | 512MB 容器 + LRU 画廊 + SSE/WS | 中等 | 内存预算化 |
| 上游生成 | 6-30s/张 | 是（不可控） | 路由优化（已 Thompson/UCB1）+ 成本智能调度（§6.5） |
| 长时间运行 | — | 未观测到泄漏 | 执行方落地时跑 `scripts/loadtest.py` + 长稳测试 + 混沌实验（§11.5） |
| 连接池 | aiosqlite + httpx | 富余 | 不动 |
| 重试风暴 | retry_policy 有限重试 | 已限 | DAG 节点级重试上限 |
| 熔断 | solver_guard + adaptive_router | 已达标 | 不动 |
| 队列积压 | PriorityQueue + 持久化 | 已达标 | 公平调度 + 预测式扩容（§7.2/§7.3） |

**没有基准数据时不虚构数字**。执行方落地 §8.1 后须跑 `scripts/loadtest.py` 给出真实 token 水位对比。

---

## 16. 安全与隐私

| 项 | 现状 | v9.0.0 改进 |
|---|---|---|
| 输入/输出处理 | `IF_MAX_REQUEST_BODY` 请求体上限 | DAG 节点参数 schema 校验 |
| 认证 | 管理 Key Bearer/X-API-Key/?api_key= | 密钥轮换自动化（§9.3） |
| 授权 | 管理写操作保留 IF_ADMIN_KEYS | 多租户 RBAC 预留（§13.3） |
| 越权 | 公益开放生图/聊天不限 Key | 设计决策（不回归） |
| 密钥管理 | IF_* 环境变量注入，不硬编码 | 热重载 + 双密钥重叠期（§9.3） |
| 日志泄密 | propagate=False + mask_key | DAG 节点参数脱敏（prompt 不全量入日志） |
| 错误信息 | 不泄露敏感数据 | 不动 |
| SQL 注入 | 参数化查询（aiosqlite） | 不动 |
| XSS | JSON API 无 inline HTML | CSP nonce 化（§9.2） |
| CSRF | API 无状态 | 不适用 |
| SSRF | IP 绑定 + 受信代理校验 | 不动 |
| 路径穿越 | 静态文件 StaticFiles | 不动 |
| 文件上传 | base64 内联 | 已有上限 |
| 依赖安全 | requirements.txt | pip-audit + SBOM/SLSA（§9.6） |
| Webhook 验签 | 无 Webhook | MCP 化后补验签（§6.2） |
| 速率限制 | L1 令牌桶 + 滑窗 + 每日限额 + 自动封禁 | 反滥用 AI 识别（§9.5） |
| 敏感数据存储 | SQLite | 不动 |
| 数据导出/删除 | 代理池订阅导出 + DLQ 清空 | DAG run 导出 + 删除 |
| 最小权限 | 管理面独立 Key 池 | 多租户 per-tenant key |
| 审计能力 | `api/audit.py` trace_id 透传 | DAG 全链路审计（§9.4） |

**本指南不复制真实密钥/令牌/密码/用户隐私数据**。

---

## 17. 可观测性与故障诊断

| 项 | 现状 | v9.0.0 改进 |
|---|---|---|
| 结构化日志 | `log_buffer.py` + `log_ws.py` + `disk_logger.py` | 不动 |
| 错误追踪 | `error_tracker.py` | 不动 |
| 指标 | Prometheus `/v1/metrics` | DAG/MCP/RAG 新指标（§11.2） |
| Trace | OTel tail-based 采样 | DAG 全链路 trace（§11.4） |
| 健康检查 | livez + healthz + readyz | agent/health + dag/health |
| 关键业务事件 | SSE/WS 事件流 | DAG 节点事件 |
| 请求关联 ID | `context.py` RequestContextMiddleware | DAG run_id 透传 |
| 用户问题定位 | `/v1/diagnostics` + `/v1/slow` | 按故障类型给 runbook 链接（§11.6） |
| 告警 | `api/alerting.py` + UptimeRobot | 多通道 + 分级（§11.3） |
| 仪表盘 | Grafana 2 仪表盘 | +agent-dag +cost 仪表盘（§11.2） |
| 日志级别 | INFO 基线 | 不动 |
| 敏感信息脱敏 | mask_key + propagate=False | DAG 参数脱敏 |
| 失败任务重放 | DLQ 重试 | DAG 节点重放 |
| 运行手册 | `docs/SOP.md` | `deploy/docs/runbooks/` 自动化（§11.6） |

---

## 18. 改进总览矩阵（P0~P3 分级，v9.0.0 全景）

| ID | 级别 | 标题 | 改动文件 | 预估工时 | 依赖 | 章节 | 风险 |
|----|------|------|---------|---------|------|------|------|
| G1 | P0 | mypy strict 扩面 db.core+db.queries | `pyproject.toml`+`api/db/` | 8h | 无 | §5.1 | L2 |
| G2 | P1 | .benchmarks 旧产物清理 | `.benchmarks/`+`.gitignore` | 1h | 无（需授权） | §5.2 | L1 |
| G3 | P2 | landing Service Worker | `landing/sw.js`+`index.html` | 3h | 无 | §5.3 | L2 |
| G4 | P1* | 智能体 DAG 基础 | 新建 `api/agent/dag.py`等 | 20h | 无 | §5.4/§6.1 | L2 |
| G5 | P3+L3 | Cloudflare 接入（方案就绪） | 域名 NS | 4h | 授权 | §5.5 | L3 |
| N1 | P1 | 文档版本对齐 | `README`+`SOP`+`计划书索引` | 1-2h | 无 | §5.6 | L1 |
| N3 | P2 | db/queries.py 804 行微拆 | `api/db/queries.py` | 1h | 无 | §5.7 | L1 |
| N2 | P2 | data/ 测试残留清理 | `data/test-*` 等 | 1h | 无（需授权） | §5.8 | L1 |
| N4 | P3 | README 外部链接标注 | `README.md` | 0.5h | 确认服务器状态 | §5.9 | L1 |
| N5 | P3 | e2e 脚本统一入口 | `frontend/package.json` | 1h | 无 | §5.10 | L1 |
| 6.1-Plus | P1* | DAG 深化（多模态+RAG+自反思） | 新建 `api/agent/tools/` | 8h | G4 | §6.1 | L2 |
| 6.2 | P2 | MCP 协议化 | 新建 `api/mcp/` | 12h | 无 | §6.2 | L2 |
| 6.3 | P2 | RAG 知识库增强 | 新建 `api/rag/` | 10h | 无 | §6.3 | L2 |
| 6.4 | P2 | 流式工具调用协议 | `api/routes/chat.py` | 8h | 6.2 | §6.4 | L2 |
| 6.5 | P3 | 成本智能调度 | `api/adaptive_router.py` | 6h | 无 | §6.5 | L2 |
| 7.1 | P2 | storage Redis 集群化 | `api/storage/redis_adapter.py` | 6h | 无 | §7.1 | L2 |
| 7.6 | P2 | adaptive_router 拆分 | `api/adaptive_router*` | 4h | 无 | §7.6 | L2 |
| 8.1 | P1 | cf_solver 多节点扩容 | `deploy/`+调参 | 1d | 无（生产需授权） | §8.1 | L1-L3 |
| 8.3 | P2 | 前端 bundle 瘦身 | `frontend/vite.config.ts` | 6h | 无 | §8.3 | L2 |
| 9.2 | P2 | CSP nonce 化 | `api/main.py`+前端 | 4h | 无 | §9.2 | L2 |
| 9.3 | P2 | 密钥轮换自动化 | `api/auth.py`+`scripts/` | 4h | 无 | §9.3 | L2 |
| 9.6 | P3 | SBOM/SLSA 供应链 | `.github/workflows/` | 4h | 无 | §9.6 | L1 |
| 10.1 | P2 | 设计系统统一 | `frontend/src/styles/tokens.css` | 16h | 无 | §10.1 | L2 |
| 10.2 | P2 | 13 页体验深化 | `frontend/src/pages/` | 12h | 10.1 | §10.2 | L2 |
| 10.3 | P2 | 暗色模式+主题 | `frontend/src/` | 4h | 10.1 | §10.3 | L2 |
| 10.4 | P2 | a11y WCAG 2.2 AA 全量 | `frontend/src/pages/` | 4h | 无 | §10.4 | L1 |
| 10.5 | P3 | 国际化 i18n | `frontend/src/i18n/` | 8h | 无 | §10.5 | L2 |
| 11.2 | P3 | Grafana DAG/SLO 仪表盘 | `deploy/grafana/dashboards/` | 4h | G4 | §11.2 | L1 |
| 11.4 | P3 | OTel 深化 | `api/telemetry.py`+`deploy/` | 6h | 无 | §11.4 | L2 |
| 11.5 | P2 | 混沌工程常态化 | `tests/chaos/experiments/` | 8h | 无 | §11.5 | L2 |
| 11.6 | P3 | SRE Runbook 自动化 | `deploy/docs/runbooks/` | 6h | 无 | §11.6 | L1 |
| 12.1-深 | P2 | mypy strict 扩到 agent/vector/storage/rag/mcp | `pyproject.toml`+各模块 | 8h | G1 | §12.1 | L2 |
| 12.2 | P2 | 测试金字塔补全 | `tests/` | 12h | 各模块 | §12.2 | L2 |
| 12.3 | P2 | E2E 真实浏览器路径 | `frontend/e2e-*.cjs` | 6h | 无 | §12.3 | L2 |
| 12.4 | P2 | 性能基准回归 | `tests/performance/`+CI | 4h | 无 | §12.4 | L1 |
| 12.5 | P2 | 安全扫描自动化 | `.github/workflows/` | 4h | 无 | §12.5 | L1 |
| 13.2 | P3 | 配额计费雏形 | 新建 `api/quota/` | 8h | 无 | §13.2 | L2 |
| 13.3 | P3 | 多租户隔离预留 | 新建 `api/tenant/` | 8h | 无 | §13.3 | L2 |

**总预估**：真实缺口收尾 ~14h + v9.0.0 核心跃迁 ~64h + 架构深化 ~24h + 性能登顶 ~24h + 安全加固 ~26h + UI/UX 登顶 ~44h + 运维 ~28h + 测试 ~34h + 成本/多租户 ~16h ≈ **274h**（约 5-7 周单人全职，多 agent 并行可压缩到 3-4 周）。

> 注：旧版指南基于过时 v8.2.3 基线列了 136h 待办，但 ~85% 已在 v8.5.0 落地。本版 274h 是**纯新增跃迁工时**（已扣除已落地项），重点是 v9.0.0 深化而非重复收尾。

---

## 19. 根本原因分析（关键项）

### 19.1 旧指南基线漂移（系统性根因）

- **表面现象**：旧版《下一步改进指南.md》基线停留在 v8.2.3，列了 136h 待办，但代码已是 v8.5.0。
- **直接原因**：旧指南写于 v8.2.3，未随 v8.3.0/v8.5.0 发版同步更新。
- **系统性原因**：发版节奏快（v8.0→v8.5.0 跨 5 个版本），但指南更新未纳入发版 SOP（`docs/SOP.md` 无"指南同步"步骤）。
- **影响半径**：执行方误用旧版会把已落地项当待办重做（storage 热路径/大文件/agent E2E/litestream/Grafana/向量/成本预测/flaky 根治）。
- **修复**：本版全面重写并覆盖旧版 + N1 把"指南同步"纳入发版 SOP。

### 19.2 DAG 跃迁的复用风险

- **表面现象**：G4 DAG 需复用 agent/vector/SSE/queue 四套子系统。
- **直接原因**：DAG 是"串联层"，不是"新建层"——它调用现有子系统而非重写。
- **系统性原因**：执行方若不遵循"只追加不重构"铁律，可能破坏现有 agent 模块。
- **影响半径**：破坏 intent/critic/memory 会导致 v8.5.0 已落地的 agent E2E 回归。
- **修复**：§6.1 明确禁区"不得改现有 agent 模块"+ AST 契约 `test_async_sync_contamination.py` 必跑 + TDD 先测后写。

### 19.3 运行时验证缺口的系统性

- **表面现象**：本指南所有"已落地"结论来自静态确认，非运行测试。
- **直接原因**：本次授权边界为"只产文档，不跑不装"。
- **系统性原因**：规划阶段无法跑测试（环境/权限约束），执行阶段才跑。
- **影响半径**：静态确认的"已落地"可能存在运行时回归未发现。
- **修复**：执行方落地前必先跑 `pytest -m "not integration and not chaos and not slow"` 建立回归基线 + 关键模块 mypy strict + ruff + 前端 vitest/build 全绿，再动手。

---

## 20. Red Team 事前反证（六个月后改进计划失败倒推）

| # | 被当成事实的未经验证假设 | 被遗漏的失败场景 | 过度乐观之处 | 触发条件 | 失败成本 | 最小证伪实验 |
|---|---|---|---|---|---|---|
| 1 | 「DAG 深化后多步任务真可用」 | 自反思循环死循环（critic 一直要求重试） | 假设 LLM critic 会收敛 | critic 阈值设错 | 资源耗尽 | 设 max_reflection_loops=3 硬上限 + 测试 |
| 2 | 「MCP 化后工具可被外部客户端消费」 | MCP 协议版本变更 / 客户端兼容性 | 假设 MCP 协议稳定 | Claude Code 升级 | 工具不可见 | 多客户端实测（Claude Code/Cursor/Continue） |
| 3 | 「RAG 提升输出质量」 | 检索噪声反而污染 prompt | 假设相似=有用 | 相似度阈值设错 | 输出变差 | LLM-as-judge 评估 RAG on/off 输出质量 |
| 4 | 「成本智能调度省钱」 | 成本降权导致低质量 provider 被选 | 假设成本低=可用 | 预算紧张时 | 质量下降 | 成本 vs 成功率权衡实测 |
| 5 | 「cf_solver 3 节点水位 3x」 | 节点间状态不同步 / Turnstile 风控升级 | 假设线性扩展 | 上游风控变 | 水位不增 | loadtest 实测 + 监控风控变化 |
| 6 | 「CSP nonce 不破坏前端」 | 前端有动态 inline script 未加 nonce | 假设前端无 inline script | CSP 上线 | 脚本被拦 | CSP report-only 模式先跑 |
| 7 | 「litestream restore 能恢复」 | R2/S3 凭证配错或 bucket 不存在 | 假设配置即生效 | 灾难恢复时 | 数据永久丢失 | restore 演练常态化（§11.1） |
| 8 | 「暗色模式只是换色」 | 设计系统未用 tokens，硬编码颜色 | 假设 tokens 已统一 | 切暗色 | 颜色错乱 | 全页 tokens 化审计 |
| 9 | 「混沌工程发现真实故障」 | 混沌实验本身破坏 CI | 假设混沌可控 | 实验失控 | CI 卡死 | 混沌实验隔离环境跑 |
| 10 | 「多租户预留零影响」 | tenant_id 列加错索引 / 查询漏过滤 | 假设预留=零影响 | 启用多租户 | 数据串 | 多租户启用前 per-tenant 隔离测试 |

**可直接开始**：G1（mypy）、G2（benchmarks）、N1（文档对齐）、G4 基础（DAG）
**应先实验**：6.3 RAG（LLM-as-judge 评估）、8.1 cf_solver（loadtest 实测）、11.1 litestream（restore 演练）
**应降低优先级**：13.3 多租户（预留即可，不实施）、10.5 i18n（landing 已双语，管理面板可后推）
**当前不建议实施**：G5 Cloudflare（L3 待域名授权）、6.4 工具执行回路（依赖 6.2 MCP 先落地）

---

## 21. 原子任务清单（T01-T40）

| 任务 ID | 目标 | 前置 | 范围 | 交付物 | 验收 | 风险 | 并行 | 完成定义 |
|---|---|---|---|---|---|---|---|---|
| T01 | mypy strict 扩面 db | 无 | `pyproject.toml`+`api/db/` | strict 4 模块 | `mypy` 0 error | L2 | 是 | 见 §5.1 |
| T02 | .benchmarks 清理 | 无（授权） | `.benchmarks/`+`.gitignore` | 仅 6 基线 png | `ls` 达标 | L1 | 是 | 见 §5.2 |
| T03 | landing SW | 无 | `landing/sw.js`+`index.html` | SW 注册+离线 | perf-audit 绿 | L2 | 是 | 见 §5.3 |
| T04 | DAG 基础 | 无 | `api/agent/dag.py`等 | 拓扑+并行+失败传播 | E2E 绿 | L2 | 是 | 见 §5.4 |
| T05 | 文档版本对齐 | 无 | `README`+`SOP`+索引 | 版本一致 | `rg` 命中 | L1 | 是 | 见 §5.6 |
| T06 | db/queries 微拆 | 无 | `api/db/queries.py` | ≤800 行 | `wc -l` 达标 | L1 | 是 | 见 §5.7 |
| T07 | DAG 深化 | T04 | `api/agent/tools/` | 多模态+RAG+自反思 | 各深化测试绿 | L2 | 是 | 见 §6.1 |
| T08 | MCP 协议化 | 无 | `api/mcp/` | MCP server+tools | E2E 绿 | L2 | 是 | 见 §6.2 |
| T09 | RAG 知识库 | 无 | `api/rag/` | indexer+retriever+injector | 各测试绿 | L2 | 是 | 见 §6.3 |
| T10 | 流式工具调用 | T08 | `api/routes/chat.py` | tool loop | test 绿 | L2 | 否 | 见 §6.4 |
| T11 | 成本智能调度 | 无 | `api/adaptive_router.py` | 成本降权 | test 绿 | L2 | 是 | 见 §6.5 |
| T12 | storage Redis 集群 | 无 | `api/storage/redis_adapter.py` | sentinel/cluster | test 绿 | L2 | 是 | 见 §7.1 |
| T13 | adaptive_router 拆分 | 无 | `api/adaptive_router*` | <800+子模块 | AST 契约绿 | L2 | 是 | 见 §7.6 |
| T14 | cf_solver 扩容 | 无（生产授权） | `deploy/`+调参 | replicas 3 | 水位 3x | L1-L3 | 是 | 见 §8.1 |
| T15 | 前端 bundle 瘦身 | 无 | `frontend/vite.config.ts` | manualChunks | <300kb gz | L2 | 是 | 见 §8.3 |
| T16 | CSP nonce 化 | 无 | `api/main.py`+前端 | nonce CSP | 无 unsafe-inline | L2 | 是 | 见 §9.2 |
| T17 | 密钥轮换 | 无 | `api/auth.py`+`scripts/` | 热重载+重叠期 | test 绿 | L2 | 是 | 见 §9.3 |
| T18 | SBOM/SLSA | 无 | `.github/workflows/` | pip-audit+sbom | CI 绿 | L1 | 是 | 见 §9.6 |
| T19 | 设计系统 | 无 | `frontend/src/styles/tokens.css` | tokens+compound | 视觉一致 | L2 | 是 | 见 §10.1 |
| T20 | 13 页深化 | T19 | `frontend/src/pages/` | 各页登顶 | vitest+E2E 绿 | L2 | 是 | 见 §10.2 |
| T21 | 暗色模式 | T19 | `frontend/src/` | data-theme | 切换正常 | L2 | 否 | 见 §10.3 |
| T22 | a11y 全量 | 无 | `frontend/src/pages/` | 0 critical | a11y-audit 绿 | L1 | 是 | 见 §10.4 |
| T23 | i18n | 无 | `frontend/src/i18n/` | zh/en 词条 | 换正常 | L2 | 是 | 见 §10.5 |
| T24 | Grafana DAG 仪表盘 | T04 | `deploy/grafana/dashboards/` | agent-dag.json | 有数据 | L1 | 是 | 见 §11.2 |
| T25 | OTel 深化 | 无 | `api/telemetry.py` | DAG trace | span 串联 | L2 | 是 | 见 §11.4 |
| T26 | 混沌工程 | 无 | `tests/chaos/experiments/` | 5 实验 | CI 周跑绿 | L2 | 是 | 见 §11.5 |
| T27 | SRE Runbook | 无 | `deploy/docs/runbooks/` | 5 runbook | 诊断给链接 | L1 | 是 | 见 §11.6 |
| T28 | mypy strict 扩 agent/vector/rag/mcp | T01 | `pyproject.toml`+各 | strict 全 | 0 error | L2 | 否 | 见 §12.1 |
| T29 | 测试金字塔补全 | 各模块 | `tests/` | +40 用例 | 全绿 | L2 | 是 | 见 §12.2 |
| T30 | E2E 真实浏览器 | 无 | `frontend/e2e-*.cjs` | +15 断言 | Playwright 绿 | L2 | 是 | 见 §12.3 |
| T31 | 性能基准回归 | 无 | `tests/performance/`+CI | benchmark 基线 | 退化告警 | L1 | 是 | 见 §12.4 |
| T32 | 安全扫描 | 无 | `.github/workflows/` | pip-audit+semgrep+gitleaks | CI 绿 | L1 | 是 | 见 §12.5 |
| T33 | 配额计费雏形 | 无 | `api/quota/` | per-key 配额 | test 绿 | L2 | 是 | 见 §13.2 |
| T34 | 多租户预留 | 无 | `api/tenant/` | tenant_id 列 | 零影响 | L2 | 是 | 见 §13.3 |
| T35 | Cloudflare 接入 | 授权 | 域名 NS | WAF+CDN | 实测 | L3 | 否 | 见 §5.5 |
| T36 | SQLite 读写分离 | 无 | `api/db/core.py`调参 | 读池扩容 | test 绿 | L1 | 是 | 见 §8.4 |
| T37 | 代理池智能调度 | 无 | `api/proxy_pool.py` | provider 亲和 | test 绿 | L2 | 是 | 见 §8.5 |
| T38 | 反滥用 AI | 无 | `api/agent/intent.py`复用 | 异常识别 | test 绿 | L2 | 是 | 见 §9.5 |
| T39 | 审计全链路 | T04 | `api/audit.py` | DAG 审计 | 查询可用 | L1 | 是 | 见 §9.4 |
| T40 | data/ 残留清理 | 无（授权） | `data/test-*` | 本地清理 | 磁盘释放 | L1 | 是 | 见 §5.8 |

**可直接开始**（第 1 波，4 agent 并行）：T01 / T02 / T05 / T06
**第 2 波**（5 agent 并行）：T04+T07 / T08 / T03 / T19 / T14
**第 3 波**（4 agent 并行）：T26 / T27 / T16+T17 / T15+T22
**长期滚动**：T09 / T10 / T11 / T25 / T28-T34

---

## 22. 多 agent 子代理并行计划

### 22.1 并行可行性

| 工作流 | 可并行？ | 理由 |
|---|---|---|
| T01 mypy strict | 独立 | 仅动 `pyproject.toml`+`api/db/*.py` |
| T02 benchmarks 清理 | 独立 | 仅动 `.benchmarks/`+`.gitignore` |
| T03 landing SW | 独立 | 仅动 `landing/` |
| T04 DAG 基础 | 独立 | 新建 `api/agent/dag.py`等，不碰现有 |
| T05 文档对齐 | 独立 | 仅动文档 |
| T06 db/queries 微拆 | 独立 | 仅动 `api/db/queries.py`（与 T01 文件不重叠——T01 动 core，T06 动 queries） |
| T08 MCP | 独立 | 新建 `api/mcp/` |
| T09 RAG | 独立 | 新建 `api/rag/` |
| T14 cf_solver 扩容 | 独立 | 仅动 `deploy/`+调参 |
| T19 设计系统 | 独立 | 新建 `frontend/src/styles/tokens.css` |

**冲突点**：T01 与 T06 都动 `api/db/`（T01 动 core.py，T06 动 queries.py）—— 文件不重叠可并行，但建议串行以降低风险；T04 与 T07 串行（T07 依赖 T04）；T10 依赖 T08；T20 依赖 T19；T21 依赖 T19；T24 依赖 T04；T28 依赖 T01。

### 22.2 agent 契约（每个子代理的节点契约）

| Agent | subagent_type | 目标 | 修改范围 | 禁区 | 交付物 | 验证 | 并行 |
|---|---|---|---|---|---|---|---|
| Agent-1 | `python-pro` | T01 mypy strict | `pyproject.toml`+`api/db/core.py`+`queries.py` | 不改 schema/签名/migrations | strict 4 模块 0 error | `mypy --strict` 0 error | 第1波 |
| Agent-2 | `refactor-cleaner` | T02 benchmarks 清理 | `.benchmarks/`+`.gitignore` | 不删 `resp-shots/` 4 png + 2 e2e png | 仅 6 基线 png | `ls` 达标 + resp-audit 绿 | 第1波 |
| Agent-3 | `doc-updater` | T05 文档对齐 | `README`+`SOP`+`计划书/README.md` | 不改源码 | 版本一致 | `rg` 命中 | 第1波 |
| Agent-4 | `python-pro` | T04 DAG 基础 | 新建 `api/agent/dag.py`等 | 不改现有 agent 模块 | DAG E2E 绿 | `pytest test_agent_dag*.py` | 第2波 |
| Agent-5 | `backend-architect` | T08 MCP | 新建 `api/mcp/` | 不破坏 `/v1/agent/*` | MCP E2E 绿 | `pytest test_mcp*.py` | 第2波 |
| Agent-6 | `frontend-developer` | T03 landing SW | `landing/sw.js`+`index.html` | 不改 Vue 组件 | SW+离线 | perf-audit 绿 | 第2波 |
| Agent-7 | `frontend-developer` | T19 设计系统 | `frontend/src/styles/tokens.css` | 不重构现有页 | tokens 统一 | 视觉一致 | 第2波 |
| Agent-8 | `backend-developer` | T14 cf_solver 扩容 | `deploy/`+调参 | 不改 solver_guard 逻辑 | 3 节点水位 3x | loadtest 实测 | 第2波 |
| Agent-9 | `security-reviewer` | T16+T17 CSP+密钥轮换 | `api/main.py`+`api/auth.py` | 不破坏现有鉴权 | nonce CSP+热重载 | curl+test 绿 | 第3波 |
| Agent-10 | `performance-engineer` | T15+T22 bundle+a11y | `frontend/vite.config.ts`+pages | 不重构 | <300kb+0 critical | perf+a11y audit 绿 | 第3波 |
| Agent-11 | `python-pro` | T09 RAG | 新建 `api/rag/` | 不改 `api/vector/` | RAG 测试绿 | `pytest test_rag*.py` | 长期 |
| Agent-12 | `architect` | T35 Cloudflare | 域名 NS | L3 待授权 | WAF+CDN | 实测 | 待授权 |

### 22.3 Critic 审查（每波完成后）

| Critic | subagent_type | 审查范围 | 禁区 |
|---|---|---|---|
| Critic-1（第1波后） | `code-reviewer`+`security-reviewer` | T01/T02/T05/T06 修改点+相邻回归 | 不改代码，每个发现带严重级+证据+复现方式 |
| Critic-2（第2波后） | `code-reviewer`+`python-reviewer` | T04/T08/T03/T19/T14+DAG/MCP 链路 | 不继承 Builder 推理 |
| Critic-3（第3波后） | `security-reviewer`+`frontend-developer` | T16/T17/T15/T22+安全/UI | 同上 |

修复-复验闭环：Builder 修 P0/P1 → Critic 聚焦复验修复点及相邻回归 → 最多 3 轮，无进展即停并报告阻塞。

### 22.4 并行调度时序

```
第 1 波（Day 1-2，4 agent 并行）真实缺口收尾
  ├─ Agent-1 mypy strict          ──┐
  ├─ Agent-2 benchmarks 清理      ──┼──→ Critic-1 审查 → 修复 → 复验
  ├─ Agent-3 文档对齐              ──┤
  └─ Agent-6 db/queries 微拆      ──┘（注：与 T01 串行更稳）
                    ↓
第 2 波（Day 3-7，5 agent 并行）v9.0.0 核心跃迁
  ├─ Agent-4 DAG 基础             ──┐
  ├─ Agent-5 MCP 协议化           ──┤
  ├─ Agent-6 landing SW            ──┼──→ Critic-2 审查 → 修复 → 复验
  ├─ Agent-7 设计系统              ──┤
  └─ Agent-8 cf_solver 扩容        ──┘
                    ↓
第 3 波（Day 8-10，3 agent 并行）运营登顶
  ├─ Agent-9 CSP+密钥轮换         ──┐
  ├─ Agent-10 bundle+a11y         ──┼──→ Critic-3 审查 → 修复 → 复验
  └─ Agent-11 RAG 知识库          ──┘
                    ↓
长期滚动（4+ 周）
  ├─ DAG 深化（T07）  ├─ 流式工具（T10）
  ├─ 成本调度（T11）  ├─ OTel 深化（T25）
  ├─ 混沌工程（T26）  ├─ SRE Runbook（T27）
  ├─ mypy strict 扩（T28）  ├─ 测试补全（T29）
  ├─ 配额计费（T33）  ├─ 多租户预留（T34）
  └─ Cloudflare 接入（T35，待授权）
                    ↓
收尾终验（v9.0.0 发版）
  └─ Evaluator：对照 §23 验收清单逐项 PASS/CONDITIONAL PASS/FAIL/BLOCKED
```

---

## 23. 分阶段路线图

### 阶段 A：真实缺口收尾（v8.5.0 → v8.6.0，1-2 天）
- **目标**：G1 mypy + G2 benchmarks + G3 landing SW + N1 文档对齐 + N3 queries 微拆
- **任务**：T01/T02/T03/T05/T06
- **验收**：mypy strict 4 模块 0 error / `.benchmarks/` 仅 6 基线 png / landing SW 离线 / 文档版本一致 / queries ≤800
- **退出条件**：上述全绿 + `pytest -m "not integration and not chaos and not slow"` 全绿
- **发版**：v8.6.0（5 处版本号同步 + CI 全绿）

### 阶段 B：v9.0.0 核心跃迁（v8.6.0 → v9.0.0，1-2 周）
- **目标**：DAG 基础+深化 + MCP + RAG + 设计系统 + cf_solver 扩容
- **任务**：T04/T07/T08/T09/T19/T14
- **验收**：DAG E2E 绿 / MCP E2E 绿 / RAG 测试绿 / 设计系统统一 / cf_solver 3x 水位
- **退出条件**：上述全绿 + Critic-2 通过
- **发版**：v9.0.0（MAJOR，新端点 `/v1/agents/run` + `/v1/mcp/*` + `/v1/gallery/similar` 已有 + 设计系统）

### 阶段 C：运营登顶（v9.0.0 → v9.3.0，1 周）
- **目标**：CSP nonce + 密钥轮换 + bundle 瘦身 + a11y 全量 + 混沌工程 + SRE Runbook
- **任务**：T16/T17/T15/T22/T26/T27
- **验收**：CSP 无 unsafe-inline / 密钥热重载 / bundle <300kb / a11y 0 critical / 混沌 CI 周跑 / Runbook 可用
- **退出条件**：上述全绿 + Critic-3 通过

### 阶段 D：长期演进（v9.3.0 → v9.5.0+，4+ 周滚动）
- **目标**：流式工具调用 + 成本智能调度 + OTel 深化 + mypy strict 全 + 配额/多租户预留 + Cloudflare（待授权）
- **任务**：T10/T11/T25/T28/T33/T34/T35
- **验收**：tool loop 绿 / 成本调度绿 / DAG trace 串联 / mypy strict 全 / 配额/多租户预留零影响 / CF 接入（待授权）
- **退出条件**：v9.5.0 发版

---

## 24. 验收标准和完成定义（v9.0.0）

| 维度 | 验收 | 证据 |
|---|---|---|
| 类型 | mypy strict 4+ 模块 0 error | `mypy --strict api/errors.py api/retry_policy.py api/db/core.py api/db/queries.py` |
| 功能 | DAG 端到端 + MCP E2E + RAG 测试 | `pytest tests/test_agent_dag*.py tests/test_mcp*.py tests/test_rag*.py` |
| 用户体验 | landing LCP<1.5s + a11y 0 critical + 暗色模式 + 设计系统统一 | `node frontend/perf-audit.cjs` + `node frontend/a11y-audit.cjs` |
| 数据 | litestream RPO 秒级 + restore 演练 | restore_drill 绿 |
| 安全 | CSP nonce + 密钥轮换 + pip-audit + SBOM | curl+test 绿 + CI 绿 |
| 性能 | cf_solver 3 节点水位 3x + bundle <300kb | `loadtest.py` + `npm run build` |
| 稳定性 | CI flaky 0 + 混沌工程周跑 | CI 连续 3 次 main 绿 |
| 测试 | 单测 80% + 集成/混沌分轮绿 + E2E 22+ + vitest 197+ | CI 全绿 |
| 可观测 | Grafana DAG/SLO 仪表盘 + OTel DAG trace | 仪表盘有数据 + span 串联 |
| 运维 | SRE Runbook + 混沌实验 | `deploy/docs/runbooks/` + `tests/chaos/experiments/` |
| 文档 | README/SOP/计划书索引版本一致 + verification-log 追加 | `rg` 命中 |
| 发布 | 版本 5 处一致 = 9.0.0 + CI 全绿 | grep 确认 |
| 回滚 | 各工作流回滚方案可用 | 见各节"回滚" |

---

## 25. 风险、阻塞项和待确认

### 25.1 风险登记簿

| 风险 | 概率 | 影响 | 缓解 | 回滚 |
|---|---|---|---|---|
| DAG 自反思循环死循环 | 中 | 高 | max_reflection_loops=3 硬上限 | 关闭自反思 |
| MCP 协议版本变更 | 中 | 中 | 双轨并存 + `IF_MCP_ENABLED=0` | 关 MCP |
| RAG 检索噪声污染 | 中 | 中 | 相似度阈值 + LLM-as-judge 评估 | `IF_RAG_ENABLED=0` |
| 成本调度降权低质量 provider | 中 | 中 | 成本 vs 成功率权衡 + 可关 | `IF_COST_AWARE_ROUTING=0` |
| cf_solver 扩容不稳 | 中 | 高 | 本地 `--scale` 验证再生产 | 单副本 |
| CSP nonce 拦截前端脚本 | 中 | 中 | CSP report-only 先跑 | CSP 关 |
| litestream 凭证配错 | 低 | 高 | restore 演练常态化 | 关 profile |
| adaptive_router 拆分破坏 import | 中 | 高 | 兼容垫片 + AST 契约 | `git revert` |
| 大文件拆分（adaptive_router 770）破坏 monkeypatch | 中 | 高 | `_pkg_attr()` 运行时读包命名空间 | `git revert` |
| 混沌实验破坏 CI | 中 | 中 | 隔离环境跑 | 关 chaos |
| 多租户预留 tenant_id 列影响 | 低 | 中 | 缺省 'default' + per-tenant 隔离测试 | `IF_MULTI_TENANT=0` |

### 25.2 阻塞项与待确认

| 项 | 说明 | 最小验证动作 |
|---|---|---|
| `imagefree.tingfengai.art` 服务器状态 | 记忆索引表明"服务器已过期"，演示链接存疑（N4） | 用户确认服务器状态 |
| Cloudflare 域名 NS 迁移授权 | G5/T35 前置（L3） | 用户确认可迁移 NS |
| cf_solver 生产扩容授权 | T14 生产部署前置（L3） | 用户授权后本地验证 → 生产 |
| .benchmarks 删除授权 | G2/T02 不可逆 | 用户授权后执行 `scripts/_bench_cleanup.cjs` |
| data/ 残留 DB 清理授权 | T40 不可逆 | 用户授权后清理 |
| litestream R2/S3 凭证 | 已有占位，真实凭证待用户提供 | 用户提供 endpoint/bucket |
| DAG 深化的真实付费边界 | planner 用 tryingopen 免费上游（已授权） | Mock 验证 + 最小真实调用 |

---

## 26. 推荐的下一步

1. **第 1 波启动**：Agent-1（mypy）+ Agent-2（benchmarks，需授权）+ Agent-3（文档对齐）+ T06（queries 微拆）—— 4 项可并行，均低风险高价值。
2. **第 1 波验证**：4 agent 完成后启动 Critic-1 审查。
3. **第 2 波启动**：Critic-1 通过后，Agent-4（DAG 基础）+ Agent-5（MCP）+ Agent-6（landing SW）+ Agent-7（设计系统）+ Agent-8（cf_solver 扩容，本地验证）—— 5 agent 并行。
4. **第 2 波验证**：Critic-2 审查 DAG/MCP 链路完整性 + 付费红线。
5. **第 3 波启动**：Critic-2 通过后，Agent-9（CSP+密钥）+ Agent-10（bundle+a11y）+ Agent-11（RAG）—— 3 agent 并行。
6. **收尾终验**：Evaluator 对照 §24 验收清单逐项 PASS。
7. **发版**：v8.5.0 → v8.6.0（阶段 A）→ v9.0.0（阶段 B）→ v9.3.0（阶段 C）→ v9.5.0（阶段 D）。

---

## 27. SaaS 专项检查（本项目适用项）

本项目是公益开放网关，非多租户 SaaS，以下项**部分适用**：

| 项 | 适用 | 说明 |
|---|---|---|
| 多租户隔离 | ⚠️ 预留 | 单租户（站长 + 公益访客），v9.5.0 预留 tenant_id（§13.3） |
| 登录/认证/会话 | ⚠️ | 无用户登录；管理 Key 固定 Bearer，v9.0.0 加密钥轮换（§9.3） |
| RBAC/ABAC | ❌ | 仅站长/访客两级（多租户预留时加 RBAC） |
| 管理员能力 | ✅ | 管理面板写操作保留管理 Key |
| 订阅生命周期 | ❌ | 无订阅（配额计费雏形 §13.2 预留） |
| 支付与账单 | ❌ | 公益免费 |
| 配额和限流 | ✅ | L1 令牌桶 + 滑窗 + 每日限额 + 自动封禁 + v9.0.0 配额雏形 |
| 审计日志 | ✅ | `api/audit.py` + v9.0.0 DAG 全链路审计 |
| 用户邀请 | ❌ | 无（多租户预留时加） |
| 团队/组织 | ❌ | 无 |
| 数据导入导出 | ⚠️ | 代理池订阅导出；v9.0.0 加 DAG run 导出 |
| 数据删除 | ⚠️ | DLQ 清空；v9.0.0 加 DAG run 删除 |
| 隐私和合规 | ✅ | landing 隐私声明/DPA + 日志脱敏 + v9.0.0 CSP nonce |
| Webhook | ⚠️ | MCP 化后补验签（§6.2） |
| 外部集成 | ✅ | 5 家上游 Provider + v9.0.0 MCP 对外 |
| 客服/问题诊断 | ⚠️ | `/v1/diagnostics` + v9.0.0 SRE Runbook 链接 |
| 灰度/功能开关 | ✅ | `IF_*_ENABLED` 缺省关闭（agent/storage/mcp/rag/quota 等） |
| 试用转付费 | ❌ | 无 |
| 取消订阅/数据保留 | ❌ | 无 |

---

## 28. 禁止事项清单（继承 + 更新）

1. **禁止把 v8.5.0 已落地项当待办重做**（storage 热路径/大文件拆分/agent E2E/litestream/Grafana/UptimeRobot/compose profiles/向量检索/成本预测/flaky 根治/前端登顶组件/a11y+perf 审计 —— 均 v8.5.0 已闭环，见 §12.8 勿重跑清单）。
2. **禁止重构公共接口**：函数名/参数/返回结构不得改，只追加新方法。
3. **禁止删除兼容垫片**：`account_pool/pool.py`/`email_pool.py` 等旧文件保留 `from .new_module import *`。
4. **禁止造轮子**：`storage/`/`agent/`/`vector/`/`solver_guard` 联邦/`adaptive_router` Thompson-UCB1/`ws_events`/`sse_events`/`cost_forecast`/`background.spawn`/`prompts/`/`skills/` 均已造好，只接线/扩面/深化。
5. **禁止改现有 agent 模块**（intent/critic/memory/guard/routes）—— DAG/MCP 只追加新文件。
6. **禁止过度工程**：不换 Postgres/不上 Kafka/不分库分表/不引入 Redis 为强依赖（触发器未出现前都是负优化，见 §2.7）。
7. **禁止破坏公益开放**：生图/聊天不限 Key 不得回归。
8. **禁止破坏单机可部署**：外部依赖必须可选（`IF_STORAGE_BACKEND=sqlite` 缺省零依赖）。
9. **禁止伪实现**：声称完成必须附 `pytest`/`vitest`/`build` 真实输出。
10. **禁止为通过测试削弱断言**。
11. **禁止为通过 mypy 大面积 `# type: ignore`**（仅 aiosqlite 内部属性允许 `# type: ignore[attr-defined]`）。
12. **禁止自动 commit/push/PR**（`.pre-commit-config.yaml` 强制 ruff check+format）。
13. **禁止真实付费上游调用**（agent/DAG/MCP 链路用 tryingopen 免费上游 + `IF_MOCK_UPSTREAM=1` Mock，测试不发起真实付费）。
14. **禁止 `.sh` 脚本**（Windows 用 node/PowerShell）。
15. **禁止 `git reset --hard`/`git clean`/force push/amend 用户历史**。
16. **禁止 `.wolf/*.md` 从 git 历史恢复**（git 历史无此文件，按 CLAUDE.md 协议重写最小版）。
17. **禁止删除 `.benchmarks/resp-shots/` 4 png + 2 e2e png**（视觉回归基线）。
18. **禁止 cf_solver 生产扩容未经本地 `--scale` 验证**。
19. **禁止 Cloudflare 擅自改域名 NS**（L3 待用户授权）。
20. **禁止 DAG planner 发起真实付费 LLM**（tryingopen 免费上游 + Mock 验证）。

---

## 29. 每条改动的 TDD 模板

```markdown
### [改动 ID] 简述

**目标**：一句话

**改动范围**：
- `file:line` 清单
- 禁区：不得动的文件/函数

**前置依赖**：依赖 [改动 ID] 先完成

**实现要点**：
- 算法/数据结构/接口设计
- 复用现有：[指出已有的轮子，见 §4]

**TDD 清单**：
1. RED：`tests/test_xxx.py::test_case_1` —— 描述断言
2. RED：`tests/test_xxx.py::test_case_2` —— 描述断言
3. GREEN：实现 `xxx` 让测试过
4. IMPROVE：重构（不改行为）

**验收命令与标准**：
```bash
pytest tests/test_xxx.py -v  # 全绿
ruff check api/xxx.py       # 0 error
mypy --strict api/xxx.py    # 0 error（新模块直接 strict）
```

**回滚方案**：`git revert <commit>` 或 `IF_XXX=legacy` 切回旧逻辑
```

---

## 30. 执行检查清单（交付前反向自检）

- [x] 本指南基于 v8.5.0 真实基线（非过时 v8.2.3），5 处版本号一致已复核
- [x] 每条缺口/新跃迁有 `file:line`/命令证据（见 §3 静态侦察 + §4 复用资产 + §5 真实缺口）
- [x] 已落地项标注"勿重做"（见 §12.8 验证过勿重跑清单 + §28 禁止事项）
- [x] v9.0.0 新跃迁每项标注复用的现有轮子（见 §6 各子项"复用轮子"）
- [x] 40 个原子任务契约齐全（T01-T40，目标/范围/禁区/交付/验证/完成定义）
- [x] 并行/串行依赖标注（§22.4 时序图 + §22.1 冲突点）
- [x] Critic 独立审查（不继承 Builder 推理，每波后审查）
- [x] 验收清单可机器校验（§24）
- [x] 风险与回滚方案齐（§25.1）
- [x] Red Team 事前反证（§20，10 个未验证假设）
- [x] 未运行测试（本次授权仅产文档，验收命令是执行方待跑清单）
- [x] 不含真实付费调用、不含 commit/push/deploy 指令
- [x] 不含 `.sh` 脚本（Windows 约束）
- [x] 不重构现有文件（只追加新文件 + 调参 + 接线）
- [x] 上游 4 份档案已消费（mtpswcm8 基线 + mtpst934 现状分析 + 落地闭环计划 + 步骤拆解细则），不重复其内容
- [x] 旧版 v8.2.3 基线段已替换为 v8.5.0 真实基线

---

> **本指南是蓝图，不是圣旨。** 执行方在落地过程中发现某条方案有更优解，应在 `docs/verification-log.md` 记录决策与理由后调整。第一性原理是「消除四重摩擦 + 把单步调用升级为多步智能体编排」，凡不服务于此的改动都是次要的；凡不附真实验证输出的「完成」都是伪实现；凡把已落地项当待办重做的都是浪费；凡不先看已有轮子就造轮子的都是反模式。保持这四条，项目就只会越迭代越好。

---

## 附录：上游档案索引（本指南消费，不重复）

| 档案 | 位置 | 用途 | 本指南消费方式 |
|---|---|---|---|
| 权威基线档案 | `计划书/2026-09-06-stage-mtpswcm8-权威基线档案.md` | v8.5.0 真实状态快照 + 5 缺口收敛 | §1.1/§2/§3/§5 消费 |
| 全栈现状分析 | `计划书/2026-09-07-stage-mtpst934-全栈现状分析.md` | v8.5.0 复核 + 5 新发现（N1-N5） | §5.6-§5.10 消费 |
| 落地闭环计划 | `计划书/全阶段落地闭环完整的步骤+多agent子代理并行计划.md` | 6 工作流 + 多 agent 并行编排 | §22 消费 + 升级 |
| 步骤拆解细则 | `计划书/全阶段落地闭环-步骤拆解细则.md` | 6 工作流原子步骤（A1/A2/B1/C1/D1/D2） | §5.1-§5.5 引用，不重复 |
| 落地闭环核实 | `计划书/2026-09-06-stage-mtpr9lhc-落地闭环核实与基线测试记录.md` | 验证记录 | §12.8 消费 |
| 测试数据采集 | `计划书/2026-09-06-stage-mtpr4smw-测试数据采集与收尾基线.md` | 测试基线 | §12 消费 |
| 测试结果汇总 | `计划书/2026-09-06-stage-mtprbdhs-测试结果汇总与收尾基线.md` | 收尾基线 | §12 消费 |

**执行方落地顺序**：先读上游 4 份档案建立基线认知 → 再读本指南 §6+ 做 v9.0.0 跃迁 → 按 §22 多 agent 并行 → 按 §23 路线图发版。

---

## 31. 侦察阶段深度补充（4 Agent 并行侦察新发现，2026-09-07）

> 本章是规划阶段并行启动的 4 个 Explore Agent（后端/前端/测试CI/文档部署）的侦察结论收敛。**所有发现均带真实 `file:line` 证据，是 §5-§17 各章未覆盖的真实新缺口**，执行方落地时优先纳入编排。侦察方法：只读静态核查（Read/Grep/Glob/ls/wc/git log），未运行测试（权限约束），标"静态确认"。

### 31.1 后端并发与错误处理缺口（后端侦察新发现）

| ID | 缺口 | 证据 | 影响 | 优先级 | 修复方向 |
|---|---|---|---|---|---|
| B1 | `api/auth.py:34-38` 聊天限流全局 `threading.Lock` 串行 | `_lock = threading.Lock()` + `_chat_buckets` dict，高并发 `check_chat_rate_limit:169-179` 持锁清理+10000 裁剪 | 聊天高并发瓶颈 | P2 | 改 asyncio.Lock 或分片锁（注意 sync→async 传染，Do-Not-Repeat） |
| B2 | `api/sse_events.py:30-36` `_hb_seq` 全局计数器非线程安全 | `_next_heartbeat_seq` 无锁，多 worker 并发心跳可能跳号 | 心跳 sequence 跳号（非致命，但破坏 Last-Event-ID 顺序） | P3 | 加 `itertools.count` 或原子自增 |
| B3 | `api/dispatch_edit.py:99-100` 文件锁竞态无重试上限 | `O_EXCL` 创建后 `_edit_mutex_stale` 失败 `os.unlink` + `except OSError: pass`，无重试上限 | 极端竞态下空转 | P3 | 加重试上限 + 退避 |
| B4 | `api/log_ws.py:69-74` 无 running loop 静默丢日志 | `broadcast_log` `except RuntimeError: pass` | 非异步线程日志丢失 | P3 | 改用 queue + 后台 loop 消费 |
| B5 | `api/handlers.py:33-41` 500 一刀切不区分错误类型 | `generic_exception_handler` 仅 `log.exception`，不区分可重试/永久错误 | 可重试错误被当永久失败，DLQ 误推 | P2 | 分类 AppError 错误码决定是否可重试 |
| B6 | `api/worker/engine.py:374-381` `except BaseException` 吞 `KeyboardInterrupt`/`SystemExit` | batch worker 捕获后 `log.exception` + mark_finished | 优雅关闭被吞 | P2 | 改 `except Exception`，re-raise BaseException |

### 31.2 后端硬编码常量债（后端侦察新发现）

| ID | 硬编码 | 证据 | 优先级 | 修复方向 |
|---|---|---|---|---|
| C1 | adaptive_router 全局常量写死 | `api/adaptive_router.py:41-49` `_MAX_RECORDS=1000/_OPEN_COOLDOWN=30.0/_OPEN_FAIL_RATIO=0.5/_OPEN_MIN_SAMPLES=5/_LATENCY_FLOOR=100.0/_INIT_EWMA=2000.0/_ALPHA=0.2` 全硬编码 | P3 | 抽 `IF_ROUTING_*` 配置（参照 §6.5 成本调度，只调参不改算法） |
| C2 | scaler 扩容阈值硬编码 | `api/worker/scaler.py:83` `up_threshold=0.6`（注释 `:81` 承认） | P3 | 抽 `IF_WORKER_SCALE_UP_THRESHOLD` |
| C3 | SSE 常量硬编码 | `api/sse_events.py:25-27` `MAX_QUEUE=50/_MAX_EVENTS_PER_TASK=50/HEARTBEAT_INTERVAL=15.0` | P3 | 抽 `IF_SSE_*` |
| C4 | Kasada 密码/salt 写死源码 | `api/providers/falai.py:54-56` `_KASADA_PASSWORD_HEX/_KASADA_SALT/_KASADA_ITERATIONS=100000`（逆向实证常量，非密钥但上游换密钥需发版） | P3 | 抽 `IF_FALAI_KASADA_*` 配置（可被 env 覆盖） |
| C5 | 号池目标 1 万硬编码 | `api/config/__init__.py:280` `nanobanana_account_target=10000` | P3 | 已是 Field 默认，可 env 覆盖，标注文档 |
| C6 | base64 目录配额 5GB | `api/config/__init__.py:254` `if_img_max_gb=5.0`（512MB 容器需调低） | P3 | 生产文档标注调低 |
| C7 | 任务硬超时 8 分钟 | `api/config/__init__.py:121` `task_hard_timeout=480`（视频生成可能不够） | P2 | 视频链路单独配置更长超时 |

### 31.3 后端测试覆盖缺口（测试侦察新发现，最严重）

| ID | 模块 | 证据 | 优先级 | 修复方向 |
|---|---|---|---|---|
| D1 | `api/background.py` **零测试** | Grep `background.py` 在 tests/ **0 引用**（spawn 强引用集，v7.7 落地） | **P1** | 新建 `tests/test_background_spawn.py`（spawn 强引用 + GC 防回收 + 异常隔离） |
| D2 | `api/provider_probe.py` **零测试** | Grep **0 引用**（provider 健康探测，lifespan:155 启动） | **P1** | 新建 `tests/test_provider_probe.py` |
| D3 | `api/cf_clearance_solver.py`(517行) 无单测 | 纯协议求解器（DEFLATE+XOR+custom_base64），逆向逻辑高风险 | **P1** | 新建 `tests/test_cf_clearance_solver.py`（协议拼装/响应解析/边界） |
| D4 | `api/email_sources/` 11 源仅 2 测试 | `custom_imap/guerrilla/linshi/mailgw/mailtm/tempmail/tempmailio/temptf` 8 源无专属测试 | P2 | 每源补契约测试（Mock 429/超时/malformed） |
| D5 | `api/cost_forecast.py` 无单测 | `predict_budget_burn`（v8.5 落地） | P2 | 新建 `tests/test_cost_forecast.py`（预测曲线/预算告警阈值） |
| D6 | `api/vector/embed.py` 无单测 | embedding 生成（v8.5 落地） | P2 | 新建 `tests/test_vector_embed.py`（SimHash 降级/sqlite-vec） |
| D7 | `api/geo_ip.py`(352行) 无单测 | IP 地理位置查询（缓存+线程池） | P3 | 新建 `tests/test_geo_ip.py`（缓存命中/线程池回源/降级） |
| D8 | `api/routes/` 13 路由仅 3 专属测试 | `generate/health/security/tasks/ecosystem/gallery` + admin `query/write` 无专属（靠 openapi_contract + 集成间接） | P2 | 逐路由补专属测试 |
| D9 | `api/account_pool/` 11 模块仅 2 测试 | `borrow/engine/scoring/signin/stats/store` 无专属 | P2 | 逐模块补 |
| D10 | `api/prompts/loader.py` 无测试 | SKILL.md 加载器 | P3 | 新建 `tests/test_prompt_loader.py` |

### 31.4 前端架构债（前端侦察新发现，最大架构债）

| ID | 缺口 | 证据 | 优先级 | 修复方向 |
|---|---|---|---|---|
| F1 | **管理面板 14 页非 13 页** | `frontend/src/pages/` 实测 14 文件（多出 `ApiGuide.tsx`，CLAUDE.md/anatomy.md 未列） | P2 | 更新 CLAUDE.md/anatomy.md 为 14 页 + 本指南 §10.2 已含 ApiGuide |
| F2 | **内联 `<style>` 散落 40+ 处** | Grep `'<style>\{' 在 `frontend/src` 命中 **40+ 处**（Layout/StatCard/Feedback/Skeleton/Dashboard/Accounts/Costs/Security/Logs 等）—— 每组件自带全局 `<style>`，靠类名约定隔离，**存在类名冲突+重复渲染开销** | **P1** | 迁 CSS Modules 或统一到 `index.css`（§10.1 设计系统统一时一并做） |
| F3 | **暗色模式跟随系统无手动切换** | `frontend/src/index.css:120-172` `@media (prefers-color-scheme: dark)`；Grep `toggleDark/setTheme/data-theme` **0 命中** | P2 | §10.3 暗色模式加 toggle 按钮（复用已有 token） |
| F4 | **管理面板零 i18n** | Grep `i18n/locale/useTranslation` 在 `frontend/src` **0 命中**（landing 有 `useI18n.js`）；全中文硬编码（ApiGuide.tsx:42-50 等） | P3 | §10.5 i18n（移植 landing `useI18n.js` 模式到 React） |
| F5 | **Dashboard.tsx 714 行偏胖** | 8 个 useApi + 画廊密码 + 图表数据加工堆一页 | P3 | 参照 Accounts 拆 6 子组件范式，拆 `DashboardStats`/`GalleryPanel` |
| F6 | **ToastHost 无 aria-live** | `frontend/src/components/ToastHost.tsx` 全文无 `aria-live`/`role="status"` | P2 | 加 `aria-live="polite"` + `role="status"`（§10.4 a11y 补全） |
| F7 | **画廊图片无 loading="lazy"** | `frontend/src/components/Gallery.tsx` 头 40 行未见 `loading="lazy"`/`srcset`/响应式 | P2 | 长列表图片补 `loading="lazy"` + 显式 width/height |
| F8 | **CommandPalette 与 Layout 双份 navConfig** | `CommandPalette.tsx:24-39` 注释自承"与 Layout 侧栏同步"但实际两份手写 | P3 | 抽 `frontend/src/navConfig.ts` 单源 |
| F9 | **fetchSlow 裸 fetch 绕过 apiFetch** | `frontend/src/pages/Slow.tsx:40-47` 注释明说"不写进 api.ts（该文件正由重构子代理处理，避免冲突）" —— 历史遗留 | P2 | 并入 `api/misc.ts` 走 `apiFetch`（30s 超时/ApiError/中文文案） |
| F10 | **Skeleton 双份** | `Feedback.tsx` 的 `Skeleton`（基础 shimmer）vs `Skeleton.tsx`（结构化骨架），Tasks.tsx 用 `as SkeletonStructured` 别名区分 | P3 | 统一到 `Skeleton.tsx`，`Feedback.Skeleton` 降级为 alias |
| F11 | **两套数据层逻辑重叠** | landing `usePolling.js`(Vue) vs frontend `useApi.ts`+`pollingScheduler.ts`(React)，轮询/竞态/visibility 逻辑高度重叠 | P3 | 不强制统一（Vue/React 跨框架），但 landing 可借鉴 pollingScheduler 单例模式 |
| F12 | **硬编码域名** | `ApiGuide.tsx:37`(fallback) + `landing/index.html:19/26/29/38/53/75`(canonical/og:url/og:image/JSON-LD) | P2 | 抽 `IF_BASE_URL` 注入（后端已有，前端用 build-time `define`） |
| F13 | **landing JSON-LD 版本漂移** | `landing/index.html:57` `softwareVersion: "7.2.0"` vs `package.json:3` `8.5.0` | P2 | vite `define __APP_VERSION__` 注入 JSON-LD（vite.config.js 已有 define，JSON-LD 静态硬编码未走） |
| F14 | **frontend 覆盖率 include 路径过期** | `frontend/vite.config.ts:44-48` `coverage.include=['src/api.ts','src/components/Feedback.tsx','src/hooks/useApi.ts']` —— `src/api.ts` 已不存在（拆成 `api/` 目录） | P2 | 修正 include 路径为 `src/api/**` + `src/components/**` + `src/hooks/**` |
| F15 | **as any 残留** | `BarChart.tsx:16` CustomTooltip `any` + `Ecosystem.tsx:240-251` 7 处 `(t.inference as any)?.cheapest_input` | P3 | 定义 `CustomTooltipProps` + `EcosystemModel` 类型 |

### 31.5 CI 与测试基线修正（测试侦察新发现）

| ID | 缺口 | 证据 | 优先级 | 修复方向 |
|---|---|---|---|---|
| E1 | **"1742 单测全绿"基线无法核实** | `.benchmarks/v81full2.xml` 实测 `tests=1723 failures=0 skipped=1`（2026-09-06 00:39 最近全绿）；memory 索引"1742"为计划书引用值 | P2 | 文档统一为 1723 真实值（本指南 §0 已修正） |
| E2 | **final_suite.py 注释与代码不一致** | `scripts/final_suite.py:5` 注释"对齐到 70"，`:12` 代码 `--cov-fail-under=80` | P3 | 修正注释为 80 |
| E3 | **sync_deploy.py 废弃但文档未更新** | `scripts/sync_deploy.py:3` 自述 v6.8.0 废弃；CI 不跑；但 CLAUDE.md:107 + 旧指南仍提"sync_deploy 防漂移" | P2 | 文档删除 sync_deploy 引用（本指南 §5.6 N1 文档对齐时一并做） |
| E4 | **CI 不跑前端 E2E** | `ci.yml` `frontend-gate` 仅跑 `tsc -b && vite build` + `vitest run`，**不跑 `e2e-smoke.cjs`/`a11y-audit.cjs`/`perf-audit.cjs`**（缺 chromium 装包） | P2 | 加 `frontend-e2e` CI job（缓存 chromium + 跑 7 个 .cjs 审计） |
| E5 | **集成/混沌轮无 cov-fail-under** | `ci.yml:84,102` 仅 `--cov=api --cov-report=term`，不合并门槛 | P3 | 加 `--cov-append` + 统一 80% 门槛 |
| E6 | **test_ws_events.py 永久 skip** | `:114` `@pytest.mark.skip` TestClient 同步 WS 不支持并发 publish+receive | P3 | 用 async WS 客户端替代（httpx.AsyncClient + websockets） |
| E7 | **ci_repro.xml 2 失败预存 flaky** | `test_config_validate::test_every_env_example_var_is_consumed`（env.example 孤儿变量 IF_AGENT_*）+ `test_ip_blocklist::test_ttl_expiry_allows_request`（`sqlite3.OperationalError: database is locked`） | P2 | env 孤儿变量补 config 映射 + DB 锁竞争用更长 busy_timeout |
| E8 | **coverage 实测 100% 但单次聚合** | `.coverage`(90KB, 2026-09-06 14:15) `measured api files: 137 / TOTAL stmts=13834 miss=2 cov=100.0%` —— 但这是单次跑出，非 CI 多轮合并 | P3 | 文档标注"行覆盖 100% 非 CI 多轮，分支覆盖未度量" |

### 31.6 文档与部署新发现（文档侦察新发现）

| ID | 缺口 | 证据 | 优先级 | 修复方向 |
|---|---|---|---|---|
| G6 | **N6 deploy/pyproject.toml 版本漂移** | `deploy/pyproject.toml:4` = `8.3.0`（vs 根 `8.5.0`）—— **mtpst934 全栈分析未发现，本侦察新发现**；SOP.md:6 写"v7.6 起版本对齐 8 处"但 deploy/pyproject 漂移 | **P1** | 同步 `deploy/pyproject.toml` 为 8.5.0（§5.6 N1 文档对齐时一并做） |
| G7 | **docs/SKILLS_PACKAGE.md 严重过时** | `:11-16` 仍写 `worker.py`/`db.py`/`config.py` 单体文件，实际 v4.3.0 起已包化为 `worker/`/`db/`/`config/` | P2 | 整段重写（目录树失效） |
| G8 | **api/vector/ + api/agent/ + cost_forecast 无文档** | PRD `07-api-reference.md` 未提 `/v1/agent/*`/`/v1/gallery/similar`/`/v1/tasks/{id}/ws`；PROVIDER_INTEGRATION_GUIDE 未提 vector；无 cost_forecast 文档 | P2 | 补 PRD + 独立模块文档 |
| G9 | **v7.x/v8.x 无 git tag** | 发版仅靠 commit message，`git tag` 仅 v2.1.0~v6.7.0 旧 tag | P3 | 发版补 `git tag vX.Y.Z`（纳入发版 SOP） |
| G10 | **release_notes v8.3.0/v8.5.0 无文件** | 根 `release_notes.md` 仍是 v4.3.0 旧内容；`docs/releases/` 仅 archive/ 子目录（v6.x） | P2 | 补 `release_notes_8.5.0.md`（§5.6 N1 一并做） |
| G11 | **OpenAPI /docs 默认全开** | `api/main.py:107-113` 未显式禁用 `docs_url`/`redoc_url`/`openapi_url`，生产 `/docs`+`/redoc`+`/openapi.json` 默认全开 | P2 | 生产 `docs_url=None` 或管理 Key 保护（`IF_DOCS_ENABLED` 开关） |
| G12 | **PRD 滞后 v7.2.0** | `docs/prd/07-api-reference.md` 版本标注 v7.2.0，未含 v8.x 新端点 | P3 | 随 §5.6 N1 文档对齐更新 |
| G13 | **workflow_status.md 不存在** | SOP.md:477 引用但根目录无文件 | P3 | 删除 SOP 引用或补文件 |
| G14 | **docs/SOP.md 版本对齐声明与实测不符** | SOP.md:6 写"v7.6 起版本对齐 8 处"，但实测 deploy/pyproject 漂移（G6） | P2 | 随 G6 修正 |

### 31.7 安全边界薄弱处补充（后端+前端侦察新发现）

| ID | 缺口 | 证据 | 优先级 | 修复方向 |
|---|---|---|---|---|
| S1 | CORS 默认 `*` + methods/headers 全放行 | `api/config/__init__.py:191` `if_cors_origins="*"` + `api/main.py:114-119` `allow_methods=["*"]`+`allow_headers=["*"]` | P2 | 生产收紧（`.env.production.example` 已收紧 origins，methods/headers 也可收紧） |
| S2 | `?api_key=` query 弱通道 | `api/auth.py:105-108` 注释自承会进 referer/浏览器历史/access 日志 | P2 | 文档标注禁用，或 deprecate（向后兼容保留） |
| S3 | `IF_ADMIN_KEY_OPEN=1` 误配风险 | `api/auth.py:59-64` 开放模式放行管理面（仅当无任何 Key 时），运维误配即暴露封禁/DLQ | P2 | 生产强制 `IF_ADMIN_KEY_OPEN=0`（`.env.production.example` 已强制） |
| S4 | solver_guard 域名不判 DNS 重绑定 | `api/solver_guard.py:34-62` `_is_private_ip` 仅防显式 IP，域名由 DNS 解析后再判（注释:50 承认） | P2 | 加 DNS 解析后二次判定 |
| S5 | `IF_MAIL_AIExtract` 邮箱内容外发第三方 | `api/config/__init__.py:284` 默认关，开启后验证码/链接提取走 tryingopen 上游（隐私边界） | P3 | 文档标注隐私边界，开启前确认 |
| S6 | `task_to_public` image_url 未脱敏 | `api/db/queries.py:760-779` 私网 IP 脱敏（:778-779），但 `image_url` 直接透传上游 URL（可能含 query 参数） | P3 | image_url 脱敏 query 参数 |

### 31.8 侦察结论对编排的影响

本次 4 Agent 并行侦察新增 **6 大类 40+ 个真实缺口**（B1-B6 并发/错误 + C1-C7 硬编码 + D1-D10 测试缺口 + F1-F15 前端债 + E1-E8 CI 修正 + G6-G14 文档 + S1-S6 安全），其中：

- **提级到 P1 的新发现**：D1（background.py 零测试）、D2（provider_probe.py 零测试）、D3（cf_clearance_solver 无单测）、F2（内联 style 40+ 处架构债）、G6（deploy/pyproject 漂移）—— 这 5 项应纳入第 1 波或第 2 波优先执行。
- **纳入既有工作流**：F2/F3/F4/F6/F7/F9/F12/F13/F14/F15 前端债随 §10.1 设计系统统一 + §10.3 暗色模式 + §10.4 a11y 补全时一并解决；E3/E10/G6/G7/G10/G14 文档随 §5.6 N1 文档对齐时一并做；S1-S6 安全随 §9.2 CSP + §9.3 密钥轮换时一并强化。
- **新增独立任务**：D1-D10 测试缺口各自新建测试文件（见 §31.3 修复方向）；B1-B6 并发修复（见 §31.1）；C1-C7 硬编码抽配置（见 §31.2）。

**执行方落地顺序更新**（在 §22.4 时序基础上）：
- 第 1 波追加：G6（deploy/pyproject 同步，随 N1）、D1/D2/D3（零测试模块补单测，可并行）
- 第 2 波追加：F2（内联 style 迁移，随设计系统）、D4/D5/D6（邮箱源/cost_forecast/vector_embed 补测试）
- 第 3 波追加：B1-B6（并发/错误处理修复）、C1-C7（硬编码抽配置）、E4（CI 前端 E2E job）

> **侦察诚实声明**：4 Agent 全部只读静态核查，未运行测试。所有"零测试覆盖""漂移""硬编码"结论基于 Grep `0 命中`/`wc -l`/`Read` 源码确认，非运行时验证。执行方落地前须先跑测试建立回归基线，再逐项验证。

---

*文档结束 · 生成于 2026-09-07 · 基于 v8.5.0 真实基线（commit accda34）全面重写 · 旧版 v8.2.3 基线段已替换 · 定位 v8.5.0 → v9.0.0 / v9.5.0 深化跃迁全景蓝图 · §31 含 4 Agent 并行侦察深度补充（40+ 新缺口，file:line 证据）*
