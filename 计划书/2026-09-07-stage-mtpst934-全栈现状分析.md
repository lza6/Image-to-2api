# 全栈现状分析（v8.5.0 复核 · 供下一步改进指南消费）

> **环节**：stage-mtpst934 —— 全栈分析（为下一步改进指南打基础）
> **生成日期**：2026-09-07
> **基线**：v8.5.0（commit `accda34`，5 处版本号一致已复核）
> **方法**：只读静态侦察（git log / ls / wc / rg / grep）+ 消化权威基线档案（mtpswcm8）与记忆索引；**未运行测试**（权限约束，全部标注静态确认）。

---

## 1. 全栈画像（复核后确认）

| 层 | 现状 | 证据 |
|---|---|---|
| 后端 | `api/` 150 py 文件，约 2.8 万行；FastAPI + uvicorn + httpx + aiosqlite | `api/routes/*` 共 65 处路由装饰器 |
| 核心链路 | `routes/generate.py` → `dispatch.py` → `worker/engine.py`(738) + `token_pool.py`(438) + `scaler.py`(261) → `sse_events.py` | worker 6 模块 |
| 路由引擎 | MAB-EWMA 自适应路由 `adaptive_router.py`(770) + 熔断 | 770 行（接近 800 上限） |
| 号池/邮箱池 | `account_pool/` 包已拆分（engine/fsm/store/scoring/signin…）+ `email_pool.py`(384) + `email_sources/` 6 源 | 已从 1111 行拆分 |
| providers | base + imagefree/aifreeforever/nanobanana/tryingopen/falai + registry + action_sniffer | 5 上游聚合 |
| agent 子系统 | `api/agent/` 6 模块 + 5 路由（`/v1/agent/*`）+ 4 Prometheus 指标；`IF_MOCK_UPSTREAM` 开关在 intent/critic/memory 3 处已实现 | v8.1.0/v8.5.0 |
| 向量检索 | `api/vector/{embed,store}.py`（SimHash 256 维 + sqlite-vec 降级）+ `/v1/gallery/similar` 路由 + 查重 | v8.5.0 |
| 成本预测 | `cost_forecast.py` + `Costs.tsx` 预算燃烧预测 + 全屏预警横幅 | v8.5.0 |
| storage | redis 适配器 + 热路径真接线（request_guard 真调 adapter.rate_limiter） | v8.3.0 |
| 前端 | React 19 + TS 5.7 + Vite 6；14 页 + 23 组件（含 chat/3 + accounts/6 子目录组件）+ 4 hooks + 17 测试文件；登顶组件（Skeleton/EmptyState/CommandPalette）已落地 | `frontend/src` |
| landing | Vue3 + three.js + @tresjs；9 个 Section 组件（Hero3D 等） | `landing/src/components` |
| 测试 | 148 文件 1857 个 `def test_`（unit≈135 + integration 13）+ chaos + performance | CI 口径 `pytest -m "not integration and not chaos and not slow" --cov-fail-under=80` |
| 部署 | docker-compose（redis/obs/backup 4 profiles）+ litestream + Grafana + prometheus + CI(ci.yml/deploy.yml) | `deploy/` |
| 数据 | 4 主 DB + 88 个 db 文件（**大量测试残留**，`data/queue.db` 29M 异常大） | `ls data/*.db` |

---

## 2. 真实缺口（mtpswcm8 档案 §4 复核确认，供改进指南编排）

| ID | 缺口 | 证据（本环节复核） | 优先级 | 工时 |
|---|---|---|---|---|
| G1 | **mypy strict 扩面 db.core + db.queries** | `pyproject.toml` strict 名单仍仅 2 模块（api.errors + api.retry_policy）；注释"215 处错误"为旧值**待验证** | P0 | ~8h |
| G2 | **.benchmarks 旧产物清理**（42 项 → 仅 6 基线 png） | `ls .benchmarks | wc -l` = 42；`scripts/_bench_cleanup.cjs` 已就绪未执行；删 42 项含 4 张已跟踪 png（git rm），需授权 | P1 | ~1h |
| G3 | **landing Service Worker** | `ls landing/sw.js` → No such file；`frontend`/`landing` 无 registerSW 引用 | P2 | ~3h |
| G4 | **智能体 DAG 编排** | `api/agent/dag.py`/`planner.py`/`api/routes/agent_dag.py` 全不存在；`rg dag api/` 无残留 | P3 | ~20h |
| ⏸ | Cloudflare 接入实施 | `deploy/docs/cloudflare-cdn.md` 存在（方案就绪，实施 L3） | P3+L3 | 待授权 |
| ⏸ | cf_solver 生产扩容 | `IF_CF_SOLVER_URLS` 配置就绪 | P1+L3 | 待授权 |

---

## 3. 本环节新发现（档案未覆盖，指南需纳入）

### N1（P1）README / SOP 版本滞后 — 文档漂移
- `README.md:9` badge 仍是 **v8.3.0**（与代码 v8.5.0 不一致）
- `docs/SOP.md:3` 版本 2.4.0 / 最后更新 2026-09-04，正文仍写"当前代码版本 **v7.7**"（:6）、回滚流程基于 v7.x 标签
- `docs/release_notes.md` 无本地根文件（`docs/releases/` 目录存在，但根 `release_notes.md` 不存在）——v5-v8.5 无本地记录，仅 git log 可溯
- `计划书/README.md` 索引仅 1 项且版本停 v7.7.6（mtpswcm8 已指出）
- 建议：小改动单（README badge + SOP 版本行 + 补 v8.5.0 摘要 + 计划书索引），~1-2h
- 建议：小改动单（README badge + SOP 版本行 + release_notes 补 v8.5.0 摘要 + 计划书索引），~1-2h

### N2（P2）data/ 测试残留 DB 膨胀（88 个 db 文件，≈65MB）
- `data/` 88 个 `*.db`，总 65MB；其中 `queue.db` **29M**、`imagefree.db` 2M，其余大量 `test-*`/`e2e*`/`probe*` 命名测试残留
- `data/*.db` 已被 `.gitignore`（:26）覆盖，`git ls-files data/` 仅 8 项（1 png+1 bak+2 shm/wal+4 ui png），**测试残留未被跟踪**（不污染仓库，但占本地磁盘）
- `queue.db` 29M 疑为持久化队列数据堆积（可能含已完成任务记录，需确认保留策略）
- 建议：定期清理本地测试残留 DB（`data/test-*`/`e2e*`/`probe*`/`inproc*`/`strict*`/`trace*`），`queue.db` 按保留策略处理；不可逆操作需授权

### N3（P2）`api/db/queries.py` 804 行超 800 上限
- 基线档案大文件清单未列（其统计截至 v8.5.0 拆分后），本环节实测 `wc -l` = 804
- 仅超 4 行，微拆或 800 上限豁免均可，低风险

### N4（P2）README 存在外部链接
- README 含 `https://imagefree.tingfengai.art` 线上演示链接，但记忆索引表明"服务器已过期/无远程部署"——链接有效性存疑，指南建议标注或移除

### N5（P3）landing/前端有 5 个 e2e 脚本残留但无统一跑法
- `frontend/e2e-smoke.cjs`/`e2e-m4.cjs`/`e2e-p3.cjs`/`e2e_real_fullstack.cjs` + `resp-audit.cjs`/`a11y-audit.cjs`/`perf-audit.cjs` 已存在但 npm scripts 未暴露（package.json 仅 smoke）；自动化审计脚本无统一入口

---

## 4. 编排建议（第 2 波指南的第 1 波执行批次）

| 波次 | 内容 | 依赖 | 验收 |
|---|---|---|---|
| 第 1 波（快赢） | A1=G1 mypy strict 4 模块 0 error · A2=G2 bench 清理（授权后）· N1 文档版本对齐 | 无 | mypy 0 error / `.benchmarks` ≤3 / README+SOP 版本一致 |
| 第 2 波 | B1=G3 landing SW · N2 data/ 清理 · N3 queries.py 微拆 | 第 1 波后 | `ls landing/sw.js` 存在 / data 残留 ≤5 |
| 第 3 波（大项） | D1=G4 agent DAG（~20h）· ⏸ CF 接入（L3 授权） | 用户确认 | agent_dag 路由 + 测试 |

---

## 5. 诚实声明

- 本环节未改源码、未 commit/push/deploy。
- 全部结论来自只读静态核查（git log/ls/wc/rg/grep）+ 消化上游基线档案与记忆；`mypy`/`pytest`/`ruff`/`node` 未运行（权限约束），标"静态确认"项不视为"已验证"。
- 执行方落地前须自行跑验收命令并附真实输出。
- 禁止把本档案列出的已落地项（§1 全栈画像）当作待办重做。

---

*文档结束 · 生成于 2026-09-07 · stage-mtpst934 全栈现状分析*
